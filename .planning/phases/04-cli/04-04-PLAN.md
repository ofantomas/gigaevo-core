---
phase: 04-cli
plan: 04
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - .claude/skills/experiment-closeout/SKILL.md
  - .claude/skills/post-experiment-fixes/SKILL.md
autonomous: true
requirements:
  - D-09
  - D-10
must_haves:
  truths:
    - "experiment-closeout promotes discovered patterns from 04_issues_log.md to PATTERNS.md Known Failures section"
    - "post-experiment-fixes generates a 06_fixes_applied.md report mapping each issue to its fix"
    - "post-experiment-fixes updates Known Failure statuses in PATTERNS.md when fixes are applied"
    - "06_fixes_applied.md includes columns for Issue, Fix Type, Files Changed, Status, and Commit/Reason"
  artifacts:
    - path: ".claude/skills/experiment-closeout/SKILL.md"
      provides: "Pattern promotion step for Known Failures"
      contains: "Known Failures"
    - path: ".claude/skills/post-experiment-fixes/SKILL.md"
      provides: "06_fixes_applied.md generation step and PATTERNS.md status update"
      contains: "06_fixes_applied.md"
  key_links:
    - from: "experiment-closeout Step 13a"
      to: "experiments/PATTERNS.md Known Failures"
      via: "Pattern promotion from issues log"
      pattern: "Known Failures"
    - from: "post-experiment-fixes Step 6"
      to: "experiments/<task>/<name>/06_fixes_applied.md"
      via: "Fix report generation"
      pattern: "06_fixes_applied.md"
---

<objective>
Add pattern promotion to experiment-closeout (D-09) and fix tracking report generation to post-experiment-fixes (D-10). After each experiment, new failure patterns flow from the issues log into PATTERNS.md, and a 06_fixes_applied.md report tracks what was fixed.

Purpose: Cross-experiment learning prevents recurrence of known failures. The fix report creates an audit trail of what was addressed vs deferred.

Output: Modified SKILL.md files for experiment-closeout and post-experiment-fixes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cli/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Known Failures promotion step to experiment-closeout SKILL.md</name>
  <files>.claude/skills/experiment-closeout/SKILL.md</files>
  <read_first>
    - .claude/skills/experiment-closeout/SKILL.md
    - experiments/PATTERNS.md
    - .planning/phases/04-cli/04-RESEARCH.md
  </read_first>
  <action>
Modify `.claude/skills/experiment-closeout/SKILL.md` to enhance the existing Step 13a (Update PATTERNS.md) with explicit instructions for promoting issues from 04_issues_log.md into the Known Failures table. Do NOT change any other steps.

**Modify Step 13a** to add Known Failures promotion instructions. The existing Step 13a already updates PATTERNS.md sections (Confirmed Patterns, Refuted Hypotheses, etc.). Add a new subsection specifically for Known Failures promotion. Replace the existing Step 13a content with:

```markdown
## Step 13a -- Update PATTERNS.md

Update `experiments/PATTERNS.md` with new findings from this experiment:
- **Confirmed Patterns**: If this experiment strengthens an existing pattern or establishes a new one
- **Refuted Hypotheses**: If this experiment refutes a hypothesis
- **Suggestive Signals**: If results are suggestive but not conclusive
- **GigaEvo Platform Failure Modes**: If new GigaEvo bugs were discovered in `04_issues_log.md`
- **Open Questions**: Update or add based on what this experiment revealed

### Known Failures promotion (D-09)

Read `experiments/$EXP/04_issues_log.md`. For each entry where "Systemic fix needed: YES":

1. Check if a matching Known Failure entry (KF-XX) already exists in PATTERNS.md
2. If YES and the fix was applied: update the entry's Status from `ACTIVE` to `FIXED (<commit>)`
3. If NO: create a new KF-XX entry in the Known Failures table with:
   - **ID**: Next sequential KF-XX number
   - **Trigger Condition**: The circumstances that cause the failure (from "Root cause" field)
   - **Symptoms**: What the user observes (from "What" field)
   - **Root Cause**: Why it happens (from "Root cause" field)
   - **Fix**: How to prevent/resolve (from "Fix applied" field)
   - **Status**: `ACTIVE` if not yet fixed systemically, `FIXED (<commit>)` if fixed
   - **Affected Types**: Which experiment types are affected (solo, adversarial, adversarial_asymmetric, prompt_coevo, all)
   - **Source**: `<task>/<name>` experiment identifier

Skip entries where "Systemic fix needed: NO" -- these are one-off issues that don't warrant a Known Failure entry.

Do NOT update agent-specific memories with experiment results -- those belong in shared stores only.
```

This preserves all existing Step 13a content while adding the structured Known Failures promotion workflow per D-09.
  </action>
  <verify>
    <automated>grep -c "Known Failures promotion" .claude/skills/experiment-closeout/SKILL.md && grep -c "KF-XX" .claude/skills/experiment-closeout/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/experiment-closeout/SKILL.md contains "Known Failures promotion" in Step 13a
    - .claude/skills/experiment-closeout/SKILL.md contains "KF-XX" (reference to Known Failure ID format)
    - .claude/skills/experiment-closeout/SKILL.md contains "Systemic fix needed: YES" (triage criterion)
    - .claude/skills/experiment-closeout/SKILL.md contains "ACTIVE" and "FIXED" (status values)
    - .claude/skills/experiment-closeout/SKILL.md contains "04_issues_log.md" in Step 13a (source reference)
    - .claude/skills/experiment-closeout/SKILL.md still contains "## Step 13a -- Update PATTERNS.md" (step header preserved)
    - .claude/skills/experiment-closeout/SKILL.md still contains "Confirmed Patterns" (existing content preserved)
    - .claude/skills/experiment-closeout/SKILL.md still contains "## Step 14 -- Completion check" (next step preserved)
    - .claude/skills/experiment-closeout/SKILL.md still contains "manifest gate complete" (completion gate preserved)
    - .claude/skills/experiment-closeout/SKILL.md still contains "Do NOT update agent-specific memories" (existing instruction preserved)
  </acceptance_criteria>
  <done>experiment-closeout Step 13a has structured Known Failures promotion workflow that reads issues log, creates new KF-XX entries, and updates existing entry statuses</done>
</task>

<task type="auto">
  <name>Task 2: Add 06_fixes_applied.md generation and PATTERNS.md status update to post-experiment-fixes</name>
  <files>.claude/skills/post-experiment-fixes/SKILL.md</files>
  <read_first>
    - .claude/skills/post-experiment-fixes/SKILL.md
    - experiments/PATTERNS.md
    - .planning/phases/04-cli/04-RESEARCH.md
  </read_first>
  <action>
Modify `.claude/skills/post-experiment-fixes/SKILL.md` to add two new capabilities: (1) generate `06_fixes_applied.md` report (D-10), and (2) update Known Failure statuses in PATTERNS.md when fixes are applied (D-09).

**Modification 1: Enhance Step 4 to also update PATTERNS.md Known Failure statuses**

After the existing Step 4 content ("For each fixed issue, update its entry: Change 'Systemic fix needed: YES' to 'Systemic fix needed: DONE'"), add:

```markdown
### Update PATTERNS.md Known Failure statuses

For each fixed issue that has a corresponding KF-XX entry in `experiments/PATTERNS.md` Known Failures table:
- Update the Status column from `ACTIVE` to `FIXED (<commit-hash>)`
- If no KF-XX entry exists for a systemic fix, create one (follow the same process as experiment-closeout Step 13a)
```

**Modification 2: Replace Step 6 (Summary) with 06_fixes_applied.md generation**

Replace the existing Step 6 content with:

```markdown
## Step 6 -- Generate 06_fixes_applied.md report

Generate a structured fix report at `experiments/$EXP/06_fixes_applied.md` (D-10). This maps each issue from 04_issues_log.md to its fix or deferral reason.

```bash
EXP='$ARGUMENTS'
PROJ="$(git rev-parse --show-toplevel)"
```

Create `experiments/$EXP/06_fixes_applied.md` with this format:

```markdown
# Fixes Applied: <task/name>

**Generated:** <date>
**Source:** 04_issues_log.md (<N> entries processed)

| # | Issue Summary | Fix Type | Files Changed | Status | Commit/Reason |
|---|--------------|----------|---------------|--------|----------------|
| 1 | <description from issues log> | <template/tool/skill/core/config/docs> | <file.py, SKILL.md, ...> | DONE | <commit hash> |
| 2 | <description> | <type> | <files> | SKIPPED | <reason: e.g., "self-resolving race condition"> |
| 3 | <description> | <type> | <files> | DEFERRED | <reason: e.g., "needs design discussion"> |

**Summary:** X fixed, Y skipped, Z deferred
**Patterns promoted to PATTERNS.md:** KF-XX, KF-YY (list IDs of new or updated Known Failure entries)
```

Populate the table from the fix list built in Step 1 (triage) and the results of Steps 2-5 (apply, test, update, commit). Each row corresponds to one entry in 04_issues_log.md. Status values:
- **DONE**: Fix was applied and committed
- **SKIPPED**: Issue was one-off, already resolved, or not worth fixing
- **DEFERRED**: Fix is needed but blocked or requires separate design work

Then present the report to the researcher as the skill's completion output.
```

**Modification 3: Add 06_fixes_applied.md to the commit in Step 5**

In Step 5 (Commit), add a note to include `06_fixes_applied.md` in the commit:

```markdown
Include `experiments/$EXP/06_fixes_applied.md` in the commit along with all fixed files.
```
  </action>
  <verify>
    <automated>grep -c "06_fixes_applied.md" .claude/skills/post-experiment-fixes/SKILL.md && grep -c "Known Failure statuses" .claude/skills/post-experiment-fixes/SKILL.md && grep -c "Patterns promoted" .claude/skills/post-experiment-fixes/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/post-experiment-fixes/SKILL.md contains "06_fixes_applied.md" (at least 3 occurrences -- Step 5 commit, Step 6 generation, format example)
    - .claude/skills/post-experiment-fixes/SKILL.md contains "Known Failure statuses" (PATTERNS.md update instruction)
    - .claude/skills/post-experiment-fixes/SKILL.md contains "ACTIVE" and "FIXED" in Step 4 context (status transition)
    - .claude/skills/post-experiment-fixes/SKILL.md contains "Patterns promoted to PATTERNS.md" (in the report format)
    - .claude/skills/post-experiment-fixes/SKILL.md contains "DONE" and "SKIPPED" and "DEFERRED" as status values in the report format
    - .claude/skills/post-experiment-fixes/SKILL.md contains "Fixes Applied:" (report title format)
    - .claude/skills/post-experiment-fixes/SKILL.md still contains "## Step 0 -- Read the issues log" (existing step preserved)
    - .claude/skills/post-experiment-fixes/SKILL.md still contains "## Step 1 -- Triage issues" (existing step preserved)
    - .claude/skills/post-experiment-fixes/SKILL.md still contains "## Step 3 -- Run tests" (existing step preserved)
    - .claude/skills/post-experiment-fixes/SKILL.md still contains "/run-tests" (test gate preserved)
    - .claude/skills/post-experiment-fixes/SKILL.md still contains "## Gotchas" (gotchas section preserved)
  </acceptance_criteria>
  <done>post-experiment-fixes generates 06_fixes_applied.md report, updates PATTERNS.md Known Failure statuses, and includes both in commit</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan modifies only Markdown skill files, not executable code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | T (Tampering) | SKILL.md files | accept | Markdown under git; changes visible in diff |
| T-04-08 | R (Repudiation) | 06_fixes_applied.md | accept | Report is generated and committed to git; provides audit trail by design |
</threat_model>

<verification>
1. `grep -c "Known Failures promotion" .claude/skills/experiment-closeout/SKILL.md` returns >= 1
2. `grep -c "06_fixes_applied.md" .claude/skills/post-experiment-fixes/SKILL.md` returns >= 3
3. `grep -c "Known Failure statuses" .claude/skills/post-experiment-fixes/SKILL.md` returns >= 1
4. Both skills preserve all existing step numbering and gates
</verification>

<success_criteria>
- experiment-closeout Step 13a has structured Known Failures promotion workflow
- post-experiment-fixes generates 06_fixes_applied.md with issue-to-fix mapping
- post-experiment-fixes updates PATTERNS.md Known Failure statuses
- No existing step logic or gates changed in either skill
</success_criteria>

<output>
After completion, create `.planning/phases/04-cli/04-04-SUMMARY.md`
</output>
