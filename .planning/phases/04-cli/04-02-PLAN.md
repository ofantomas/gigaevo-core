---
phase: 04-cli
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - .claude/skills/experiment-implement/SKILL.md
  - .claude/skills/experiment-launch/SKILL.md
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
must_haves:
  truths:
    - "experiment-implement has GSD plan generation steps (4a, 4b, 4c) replacing the old superpowers:writing-plans reference"
    - "experiment-launch has GSD plan generation steps (0a, 0b, 0c) before existing Step 0"
    - "Generated plans are saved to experiments/<task>/<name>/plans/ directory"
    - "A human approval gate exists before plan execution in both skills"
    - "Only experiment-implement and experiment-launch get GSD planning (no other skills)"
    - "The GSD plan in experiment-implement covers Steps 5-10 scope only (not Steps 11+)"
    - "experiment-launch auto-appends EVENT entries to 04_issues_log.md after launch and watchdog start"
  artifacts:
    - path: ".claude/skills/experiment-implement/SKILL.md"
      provides: "GSD plan generation and execution steps"
      contains: "implement-PLAN.md"
    - path: ".claude/skills/experiment-launch/SKILL.md"
      provides: "GSD plan generation, execution steps, and event auto-capture"
      contains: "launch-PLAN.md"
  key_links:
    - from: ".claude/skills/experiment-implement/SKILL.md Step 4a"
      to: "experiments/<task>/<name>/plans/implement-PLAN.md"
      via: "Plan auto-generation from 01_design.md"
      pattern: "implement-PLAN.md"
    - from: ".claude/skills/experiment-launch/SKILL.md Step 0a"
      to: "experiments/<task>/<name>/plans/launch-PLAN.md"
      via: "Plan auto-generation from experiment.yaml"
      pattern: "launch-PLAN.md"
    - from: ".claude/skills/experiment-launch/SKILL.md event capture"
      to: "experiments/<task>/<name>/04_issues_log.md"
      via: "bash cat >> append after launch and watchdog steps"
      pattern: "cat >> .*04_issues_log"
---

<objective>
Wire GSD plan generation and execution into experiment-implement (Steps 4a-4c) and experiment-launch (Steps 0a-0c), replacing the old superpowers:writing-plans approach with structured GSD plans per D-01 through D-05. Also add event auto-capture to experiment-launch for launch and watchdog events per D-06.

Purpose: Structured GSD plans make experiment implementation repeatable and auditable -- each implementation produces a PLAN.md with atomic tasks, verification criteria, and a SUMMARY.md after execution. Event auto-capture in the launch skill ensures launch events are recorded in the issues log.

Output: Modified SKILL.md files for experiment-implement and experiment-launch with GSD plan steps and launch event capture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cli/04-RESEARCH.md

<interfaces>
<!-- Key references the executor needs from research -->

Plan path convention (D-02):
  experiments/<task>/<name>/plans/implement-PLAN.md
  experiments/<task>/<name>/plans/implement-SUMMARY.md
  experiments/<task>/<name>/plans/launch-PLAN.md
  experiments/<task>/<name>/plans/launch-SUMMARY.md

GSD plan scope for experiment-implement (from research Pitfall 3):
  Plan covers: Steps 5-10 (code, config, manifest, launch.sh, watchdog, treatment verification)
  Plan does NOT cover: Steps 11+ (smoke test, status update, commit) -- these remain as explicit skill steps

Design doc sections that map to plan tasks (D-03):
  Treatment specification -> Code implementation tasks
  Config specification -> Config file creation tasks
  Control invariants -> Verification tasks
  Treatment verification -> Smoke test verification tasks

Event auto-capture points in experiment-launch (from RESEARCH.md):
  After Step 7 (launch.sh execution): Launch started event
  After Step 10 (watchdog start): Watchdog started event
  Any step failure: Launch failed event (handled via Gotchas section)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire GSD plan generation into experiment-implement SKILL.md</name>
  <files>.claude/skills/experiment-implement/SKILL.md</files>
  <read_first>
    - .claude/skills/experiment-implement/SKILL.md
    - .planning/phases/04-cli/04-RESEARCH.md
    - experiments/PATTERNS.md
  </read_first>
  <action>
Modify `.claude/skills/experiment-implement/SKILL.md` to replace the existing Step 4a with GSD plan generation steps (4a, 4b, 4c). Keep all other steps exactly as they are -- same numbering, same content, same gates.

**Replace the existing Step 4a** (lines containing "## Step 4a -- Plan implementation with superpowers:writing-plans" and its body) with the following three new steps:

```markdown
## Step 4a -- Generate GSD implementation plan

Read `experiments/$EXP/01_design.md` and `experiments/$EXP/codebase_map.md` (if exists from design phase).
Read `experiments/PATTERNS.md` section "Known Failures" for failure patterns relevant to this experiment type.

Create the plan directory and generate a structured implementation plan:

```bash
EXP='$ARGUMENTS'
PROJ="$(git rev-parse --show-toplevel)"
mkdir -p "$PROJ/experiments/$EXP/plans"
```

Generate `experiments/$EXP/plans/implement-PLAN.md` following the GSD plan format. Parse 01_design.md to create tasks in these categories:

1. **Code tasks**: Create/modify experiment-specific code (pipeline configs, validate.py, prompts). Map from the Treatment specification section of 01_design.md.
2. **Config tasks**: Fill experiment.yaml fields (runs, servers, config, custom_env). Map from the Config specification section.
3. **Script tasks**: Generate launch.sh, write watchdog config, write test eval script.
4. **Verification tasks**: Treatment verification checks, implementation alignment check. Map from the Control invariants and Treatment verification sections.

Each task must have:
- Explicit file paths (from `codebase_map.md` if available, otherwise from 01_design.md treatment specification)
- Concrete acceptance criteria (what grep/command proves the task is done)
- Known failure avoidance (reference KF-XX entries from PATTERNS.md that apply to this experiment type)

**Plan scope boundary**: The plan covers the work currently in Steps 5a through 10b (code, config, manifest, launch.sh, watchdog, treatment verification, alignment check). Steps 11+ (smoke test, status update, commit) remain as explicit skill steps OUTSIDE the plan. The plan's goal is: "produce all artifacts needed for smoke test."

## Step 4b -- Researcher approves implementation plan

Present the generated plan summary to the researcher:
- Number of tasks and estimated scope
- Files to be created/modified
- Known failure mitigations included (which KF-XX entries were addressed)
- Plan scope: "This plan covers implementation through treatment verification. Smoke test and commit are separate skill steps after plan execution."

Ask: "Implementation plan generated at `experiments/$EXP/plans/implement-PLAN.md`. Review and approve? (yes / revise / abort)"

- **yes**: Proceed to Step 4c
- **revise**: Incorporate feedback and regenerate the plan, then re-present
- **abort**: Stop the skill entirely

Do NOT proceed without explicit approval. This is a human gate (D-04).

## Step 4c -- Execute implementation plan

Execute each task in `experiments/$EXP/plans/implement-PLAN.md` sequentially. For each task:
1. Read the task specification (files, action, acceptance criteria)
2. Implement the change as specified
3. Run the verification check from the task
4. If the task modifies files, commit atomically with message: `feat($EXP): <task description>`

After all plan tasks complete, create `experiments/$EXP/plans/implement-SUMMARY.md` documenting:
- Tasks completed (count and names)
- Files created/modified
- Known failures avoided
- Any deviations from the plan

Then continue to Step 5 (GitNexus impact check) as normal.
```

**Do NOT modify** any other steps (Steps 0-4, 5-15, or Gotchas). Only replace Step 4a content and insert Steps 4b and 4c after it.
  </action>
  <verify>
    <automated>grep -c "implement-PLAN.md" .claude/skills/experiment-implement/SKILL.md && grep -c "Step 4b" .claude/skills/experiment-implement/SKILL.md && grep -c "Step 4c" .claude/skills/experiment-implement/SKILL.md && grep -c "superpowers:writing-plans" .claude/skills/experiment-implement/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/experiment-implement/SKILL.md contains "## Step 4a -- Generate GSD implementation plan"
    - .claude/skills/experiment-implement/SKILL.md contains "## Step 4b -- Researcher approves implementation plan"
    - .claude/skills/experiment-implement/SKILL.md contains "## Step 4c -- Execute implementation plan"
    - .claude/skills/experiment-implement/SKILL.md contains "implement-PLAN.md" (at least 3 occurrences)
    - .claude/skills/experiment-implement/SKILL.md contains "implement-SUMMARY.md"
    - .claude/skills/experiment-implement/SKILL.md contains "experiments/$EXP/plans/" (the plan directory path per D-02)
    - .claude/skills/experiment-implement/SKILL.md contains "Known Failures" (reference to PATTERNS.md per D-08)
    - .claude/skills/experiment-implement/SKILL.md contains "01_design.md" in Step 4a (per D-03)
    - .claude/skills/experiment-implement/SKILL.md contains "approve" or "approval" in Step 4b (per D-04)
    - .claude/skills/experiment-implement/SKILL.md does NOT contain "superpowers:writing-plans" (old approach removed)
    - .claude/skills/experiment-implement/SKILL.md still contains "## Step 5 -- GitNexus impact check" (existing steps preserved)
    - .claude/skills/experiment-implement/SKILL.md still contains "## Step 11 -- Smoke test" (plan scope stops before this)
    - .claude/skills/experiment-implement/SKILL.md still contains "## Step 15 -- Completion check" (last step preserved)
    - .claude/skills/experiment-implement/SKILL.md still contains "manifest gate preregistered" in Step 0 (gate preserved)
  </acceptance_criteria>
  <done>experiment-implement SKILL.md has Steps 4a/4b/4c for GSD plan generation, approval, and execution; old superpowers:writing-plans reference is removed; all other steps are unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Wire GSD plan generation and event auto-capture into experiment-launch SKILL.md</name>
  <files>.claude/skills/experiment-launch/SKILL.md</files>
  <read_first>
    - .claude/skills/experiment-launch/SKILL.md
    - .planning/phases/04-cli/04-RESEARCH.md
    - experiments/_template/04_issues_log.md
  </read_first>
  <action>
Modify `.claude/skills/experiment-launch/SKILL.md` to: (A) add GSD plan generation steps (0a, 0b, 0c) BEFORE the existing Step 0, and (B) add event auto-capture bash snippets after Step 7 (launch) and Step 10 (watchdog), plus a failure event note in the Gotchas section. This addresses both D-01/D-05 (GSD planning) and D-06 (event auto-capture for launch events).

**Part A: Insert GSD plan steps BEFORE the existing "## Step 0 -- Gate check"**:

```markdown
## Step 0a -- Generate GSD launch plan

Read `experiments/$EXP/experiment.yaml` for run definitions, server assignments, and DB allocations.
Read `experiments/PATTERNS.md` section "Known Failures" for launch-time failure patterns.

Create the plan directory and generate a structured launch plan:

```bash
EXP='$ARGUMENTS'
PROJ="$(git rev-parse --show-toplevel)"
mkdir -p "$PROJ/experiments/$EXP/plans"
```

Generate `experiments/$EXP/plans/launch-PLAN.md` following the GSD plan format. The launch plan is a checklist-style plan with tasks for:

1. **Preflight task**: Run `preflight_check.py`, verify exit code 0, list any warnings
2. **Config dump task**: For each run, dump resolved config via `--cfg job`, verify critical parameters (pipeline, problem.name, llm_base_url, model_name)
3. **Environment capture task**: Record `pip freeze`, capture server model identities, commit pre-launch artifacts
4. **Launch task**: Execute launch.sh, verify PIDs alive, set status to running
5. **Watchdog task**: Start watchdog, verify 10s survival, start anomaly detector and checkpoint crons

Each task should reference applicable KF-XX entries from PATTERNS.md Known Failures (e.g., KF-02 for Hydra interpolation in launch.sh, KF-03 for missing population_role).

**Plan scope**: The launch plan covers the full launch sequence (existing Steps 0-12). It is more mechanical than the implement plan -- mostly verification and execution of existing scripts.

## Step 0b -- Researcher reviews launch plan

Present the generated plan summary to the researcher:
- Number of runs and their server/DB assignments
- Known failure checks included
- Estimated launch sequence

Ask: "Launch plan generated at `experiments/$EXP/plans/launch-PLAN.md`. Review and approve? (yes / revise / abort)"

Do NOT proceed without explicit approval. This is a human gate (D-04).

## Step 0c -- Execute launch plan

Execute each task in `experiments/$EXP/plans/launch-PLAN.md` sequentially. Each task corresponds to the existing skill steps below -- the plan adds structure, verification criteria, and known-failure checks on top of the existing step logic.

After all plan tasks complete, create `experiments/$EXP/plans/launch-SUMMARY.md` documenting:
- Tasks completed
- Preflight result
- Runs launched (labels, PIDs, DBs)
- Watchdog status
- Any known failure mitigations triggered
```

**Part B: Add event auto-capture at three points in the existing skill steps.**

**Point 1: After Step 7 (Launch via launch.sh) -- log launch started event**

Insert a new bash block at the END of Step 7, after the existing `echo "" | bash "experiments/$EXP/launch.sh"` command. Add:

```markdown
Log the launch event:

```bash
EXP='$ARGUMENTS'
PROJ="$(git rev-parse --show-toplevel)"
LOG="$PROJ/experiments/$EXP/04_issues_log.md"
[ ! -f "$LOG" ] && cp "$PROJ/experiments/_template/04_issues_log.md" "$LOG"

cat >> "$LOG" << ENTRY

### [EVENT $(date -u +%Y-%m-%dT%H:%M:%SZ)] -- Experiment launched

- **When**: $(date -u +%Y-%m-%dT%H:%M:%SZ)
- **What**: launch.sh executed for $EXP. PIDs pending verification in Step 8.
- **Category**: launch
- **Impact**: Automated capture.
ENTRY
```
```

**Point 2: After Step 10 (Start watchdog) -- log watchdog started event**

Insert a new bash block at the END of Step 10, after the existing watchdog verification and PID recording. Add:

```markdown
Log the watchdog event:

```bash
EXP='$ARGUMENTS'
PROJ="$(git rev-parse --show-toplevel)"
LOG="$PROJ/experiments/$EXP/04_issues_log.md"

cat >> "$LOG" << ENTRY

### [EVENT $(date -u +%Y-%m-%dT%H:%M:%SZ)] -- Watchdog started

- **When**: $(date -u +%Y-%m-%dT%H:%M:%SZ)
- **What**: Watchdog started (PID $WD_PID). 10s survival verified.
- **Category**: watchdog
- **Impact**: Automated capture.
ENTRY
```
```

**Point 3: In the Gotchas section -- add launch failure event capture instruction**

Add the following bullet to the existing Gotchas section, after the existing "Issues log" bullet:

```markdown
- **Event auto-capture on failure** -- If any step fails during launch (preflight, PID verification, watchdog crash), log a failure event to `04_issues_log.md` before stopping:
  ```bash
  EXP='$ARGUMENTS'
  PROJ="$(git rev-parse --show-toplevel)"
  LOG="$PROJ/experiments/$EXP/04_issues_log.md"
  [ ! -f "$LOG" ] && cp "$PROJ/experiments/_template/04_issues_log.md" "$LOG"
  cat >> "$LOG" << ENTRY

  ### [EVENT $(date -u +%Y-%m-%dT%H:%M:%SZ)] -- Launch failed at Step <N>

  - **When**: $(date -u +%Y-%m-%dT%H:%M:%SZ)
  - **What**: Launch failed at Step <N>: <brief description of failure>
  - **Category**: launch
  - **Impact**: <describe impact, e.g., "Experiment not started. Requires fix before retry.">
  ENTRY
  ```
```

**Do NOT modify** any existing step logic (Steps 0-13 or other Gotchas items). Only insert Steps 0a, 0b, 0c before Step 0 and append event capture snippets to Steps 7 and 10, plus the Gotchas addition.
  </action>
  <verify>
    <automated>grep -c "launch-PLAN.md" .claude/skills/experiment-launch/SKILL.md && grep -c "Step 0a" .claude/skills/experiment-launch/SKILL.md && grep -c "Step 0b" .claude/skills/experiment-launch/SKILL.md && grep -c "Step 0c" .claude/skills/experiment-launch/SKILL.md && grep -c "\[EVENT" .claude/skills/experiment-launch/SKILL.md && grep -c "04_issues_log" .claude/skills/experiment-launch/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/experiment-launch/SKILL.md contains "## Step 0a -- Generate GSD launch plan"
    - .claude/skills/experiment-launch/SKILL.md contains "## Step 0b -- Researcher reviews launch plan"
    - .claude/skills/experiment-launch/SKILL.md contains "## Step 0c -- Execute launch plan"
    - .claude/skills/experiment-launch/SKILL.md contains "launch-PLAN.md" (at least 3 occurrences)
    - .claude/skills/experiment-launch/SKILL.md contains "launch-SUMMARY.md"
    - .claude/skills/experiment-launch/SKILL.md contains "experiments/$EXP/plans/" (plan directory path per D-02)
    - .claude/skills/experiment-launch/SKILL.md contains "Known Failures" (reference to PATTERNS.md)
    - .claude/skills/experiment-launch/SKILL.md contains "approve" or "approval" in Step 0b
    - .claude/skills/experiment-launch/SKILL.md contains "[EVENT" (at least 3 occurrences -- launch started, watchdog started, launch failed)
    - .claude/skills/experiment-launch/SKILL.md contains "cat >> " followed by "04_issues_log" (append operation for event capture)
    - .claude/skills/experiment-launch/SKILL.md contains "Experiment launched" (launch event description)
    - .claude/skills/experiment-launch/SKILL.md contains "Watchdog started" (watchdog event description)
    - .claude/skills/experiment-launch/SKILL.md contains "Launch failed" (failure event description)
    - .claude/skills/experiment-launch/SKILL.md contains "Category**: launch" (event category)
    - .claude/skills/experiment-launch/SKILL.md contains "Category**: watchdog" (event category)
    - .claude/skills/experiment-launch/SKILL.md still contains "## Step 0 -- Gate check: status must be implemented" (existing step preserved)
    - .claude/skills/experiment-launch/SKILL.md still contains "## Step 7 -- Launch via launch.sh" (launch step preserved)
    - .claude/skills/experiment-launch/SKILL.md still contains "## Step 13 -- Completion check" (last step preserved)
    - .claude/skills/experiment-launch/SKILL.md still contains "manifest gate implemented" in Step 0 (gate preserved)
    - Step 0a appears BEFORE Step 0 in the file
  </acceptance_criteria>
  <done>experiment-launch SKILL.md has Steps 0a/0b/0c for GSD plan generation, approval, and execution inserted before existing Step 0; event auto-capture snippets at Steps 7 (launch started), 10 (watchdog started), and Gotchas (launch failed); all existing steps unchanged</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan modifies only Markdown skill files, not executable code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | T (Tampering) | SKILL.md files | accept | Markdown under git; changes visible in diff; no executable code modified |
| T-04-04 | D (Denial of Service) | Plan generation steps | accept | Plan generation is Claude reading docs and writing Markdown; no resource-intensive operations |
</threat_model>

<verification>
1. `grep -c "implement-PLAN.md" .claude/skills/experiment-implement/SKILL.md` returns >= 3
2. `grep -c "launch-PLAN.md" .claude/skills/experiment-launch/SKILL.md` returns >= 3
3. `grep -c "superpowers:writing-plans" .claude/skills/experiment-implement/SKILL.md` returns 0
4. `grep -c "\[EVENT" .claude/skills/experiment-launch/SKILL.md` returns >= 3 (launch, watchdog, failure events)
5. `grep -c "04_issues_log" .claude/skills/experiment-launch/SKILL.md` returns >= 3 (event capture points)
6. Both SKILL.md files preserve all existing gate checks and step numbering
</verification>

<success_criteria>
- experiment-implement has Steps 4a/4b/4c replacing old superpowers:writing-plans
- experiment-launch has Steps 0a/0b/0c inserted before existing steps
- experiment-launch has event auto-capture at Steps 7 (launch started), 10 (watchdog started), and Gotchas (launch failed) per D-06
- Both skills reference plans/ directory path per D-02
- Both skills have human approval gates per D-04
- No other lifecycle skills were modified (D-05)
- Plan scope boundary is clear (implement plan covers Steps 5-10 only)
</success_criteria>

<output>
After completion, create `.planning/phases/04-cli/04-02-SUMMARY.md`
</output>
