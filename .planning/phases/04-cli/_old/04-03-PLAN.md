---
phase: 04-cli
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - gigaevo/cli/plot_group.py
  - gigaevo/cli/export.py
  - tests/cli/test_plot_cmd.py
  - tests/cli/test_export_cmd.py
autonomous: true
requirements:
  - CLI-03
  - PLT-01
  - PLT-02
  - PLT-03

must_haves:
  truths:
    - "gigaevo plot comparison --run A --run B --output-dir ./plots/ produces PNG/PDF/SVG files"
    - "gigaevo plot trajectory --run prefix@4:O --output-dir ./plots/ produces trajectory plot"
    - "gigaevo export csv --run prefix@4:O --output-file out.csv exports full evolution data"
    - "gigaevo export frontier --run prefix@4:O --output-file out.csv exports frontier-only CSV"
    - "Plot commands use same matplotlib style as tools/comparison.py"
    - "All plot and export subcommands work in both --experiment and --run mode (CLI-03)"
  artifacts:
    - path: "gigaevo/cli/plot_group.py"
      provides: "plot sub-group with comparison and trajectory subcommands"
      contains: "click.group"
    - path: "gigaevo/cli/export.py"
      provides: "export sub-group with csv and frontier subcommands"
      contains: "click.group"
    - path: "tests/cli/test_plot_cmd.py"
      provides: "Plot command tests"
    - path: "tests/cli/test_export_cmd.py"
      provides: "Export/CSV command tests"
  key_links:
    - from: "gigaevo/cli/plot_group.py"
      to: "tools/comparison.py"
      via: "Reuses plotting logic pattern (fetch data -> prepare -> plot)"
      pattern: "matplotlib"
    - from: "gigaevo/cli/export.py"
      to: "tools/redis2pd.py"
      via: "Reuses CSV export pattern (fetch programs -> pandas -> CSV)"
      pattern: "fetch_evolution_dataframe|RedisProgramStorage"
---

<objective>
Implement plotting and export subcommands: `gigaevo plot comparison`, `gigaevo plot trajectory` (visualizations), and `gigaevo export csv` / `gigaevo export frontier` (data export). Matplotlib is imported lazily only when these commands are invoked.

Purpose: Researchers need fitness curves and CSV data for analysis and papers. These commands replace tools/comparison.py, tools/plot.py (the old CLI version), and tools/redis2pd.py.
Output: Plot sub-group and export sub-group, each with tests verifying file output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@gigaevo/cli/__init__.py
@gigaevo/cli/output_formatter.py
@gigaevo/cli/run_resolver.py
@tools/comparison.py
@tools/redis2pd.py
@gigaevo/cli/plot.py

<interfaces>
<!-- From 04-01 (predecessor plan) -->

From gigaevo/cli/__init__.py:
```python
# ctx.obj dict keys:
# "formatter": OutputFormatter
# "experiment": str | None
# "runs": tuple[str, ...]
# "redis_host": str
# "redis_port": int
```

From gigaevo/cli/run_resolver.py:
```python
class RunResolver:
    @staticmethod
    def resolve(experiment, runs, redis_host, redis_port) -> list[RunConfig]: ...
```

From tools/utils.py (used for data fetching):
```python
@dataclass
class RedisRunConfig:
    redis_host: str
    redis_port: int
    redis_db: int
    redis_prefix: str
    label: str | None = None
    def url(self) -> str: ...
    def display_label(self) -> str: ...

async def fetch_evolution_dataframe(config: RedisRunConfig, add_stage_results=False) -> pd.DataFrame: ...
async def prepare_iteration_dataframe(df: pd.DataFrame) -> pd.DataFrame: ...
async def add_frontier_from_redis_to_dataframe(config: RedisRunConfig, df: pd.DataFrame) -> pd.DataFrame: ...
```

From gigaevo/monitoring/run_spec.py:
```python
@dataclass(frozen=True)
class RunSpec:
    prefix: str
    db: int
    label: str
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Plot sub-group (comparison + trajectory) with tests</name>
  <files>
    gigaevo/cli/plot_group.py,
    tests/cli/test_plot_cmd.py
  </files>
  <read_first>
    gigaevo/cli/__init__.py,
    gigaevo/cli/output_formatter.py,
    gigaevo/cli/run_resolver.py,
    gigaevo/cli/plot.py,
    tools/comparison.py
  </read_first>
  <behavior>
    Comparison:
    - comparison_produces_files: `gigaevo plot comparison --run p@1:A --run p@2:B --output-dir /tmp/test_plots` creates PNG, PDF, SVG files
    - comparison_requires_output_dir: Missing --output-dir produces error
    - comparison_single_run: Single --run works (just one line on plot)
    - comparison_uses_run_resolver: Implementation uses RunResolver to parse --run flags

    Trajectory plot:
    - trajectory_plot_produces_file: `gigaevo plot trajectory --run p@1:A --output-dir /tmp/test_plots` creates trajectory plot files
    - trajectory_plot_options: --metric, --no-best, --no-mean, --no-std flags accepted
    - trajectory_plot_format: --pdf flag produces PDF output

    General:
    - matplotlib_lazy: Importing gigaevo.cli.plot_group module does NOT import matplotlib at module level
  </behavior>
  <action>
    1. Rewrite `gigaevo/cli/plot_group.py` as a Click sub-group:
       ```python
       import click

       @click.group()
       def plot():
           """Generate plots from evolution runs."""
           pass
       ```

    2. Add `comparison` subcommand inside plot_group.py:
       - `@plot.command()`
       - Options: `--output-dir` / `-o` (required, type=click.Path()), `--smoothing` (choice: lowess/ema/savgol/gaussian/rolling/none, default="lowess"), `--window` (int, default=5), `--show` (flag), `--latex` (flag), `--metric` (str, default="fitness")
       - Implementation:
         a. Get runs from ctx.obj, resolve with RunResolver
         b. LAZY import: `import matplotlib; matplotlib.use("Agg"); import matplotlib.pyplot as plt`
         c. For each run config, build a `RedisRunConfig` from RunSpec fields (bridge between monitoring.RunSpec and tools.utils.RedisRunConfig)
         d. Call `asyncio.run(_load_runs(configs))` to fetch DataFrames (reuse the `fetch_evolution_dataframe` + `prepare_iteration_dataframe` from tools/utils.py)
         e. Apply plotting logic from tools/comparison.py: `_configure_plotting_style()`, iterate runs, smooth data, plot lines with bands
         f. Save to output-dir as `evolution_runs_comparison.{png,pdf,svg}`
         g. Echo JSON summary: `{"plots": [paths], "runs": N}`
       - Note: This command imports tools/utils.py for `fetch_evolution_dataframe` and `prepare_iteration_dataframe`. This is acceptable since tools/ are not being modified, just imported from.

    3. Add `trajectory` subcommand inside plot_group.py:
       - `@plot.command("trajectory")`
       - Options: `--output-dir` / `-o` (required), `--metric` (default="fitness"), `--pdf` (flag), `--no-best` (flag), `--no-mean` (flag), `--no-std` (flag)
       - Implementation reuses the pattern from the old gigaevo/cli/plot.py:
         a. Resolve single run from RunResolver
         b. Lazy import matplotlib
         c. Fetch trajectory data from Redis (gen-mean + frontier entries)
         d. Compute per-iteration stats
         e. Plot best line + mean line + std band
         f. Save to output-dir

    4. Write `tests/cli/test_plot_cmd.py`:
       - Use `click.testing.CliRunner`
       - For comparison test: Mock `fetch_evolution_dataframe` to return a small DataFrame with iteration and fitness columns, verify output files created in tmp_path
       - For trajectory test: Mock Redis reads (or use fakeredis) to return metric entries, verify plot file created
       - Verify matplotlib is not imported at module level: `import gigaevo.cli.plot_group; assert "matplotlib" not in sys.modules` (before invoking any command)
       - Use `tmp_path` fixture for output directories
  </action>
  <verify>
    <automated>cd /mnt/virtual_ai0001071-04017_SR004-nfs1/CFS-SR008/workspace/mathemage/gigaevo-core-internal && PYTHONPATH=. /home/jovyan/.mlspace/envs/evo/bin/python3 -m pytest tests/cli/test_plot_cmd.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep "click.group" gigaevo/cli/plot_group.py` finds the sub-group definition
    - `grep "@plot.command" gigaevo/cli/plot_group.py` finds at least 2 subcommands (comparison + trajectory)
    - `grep -c "def test_" tests/cli/test_plot_cmd.py` returns >= 5
    - `grep "matplotlib.use.*Agg" gigaevo/cli/plot_group.py` confirms headless backend
    - `grep "output-dir\|output_dir" gigaevo/cli/plot_group.py` confirms output directory option
    - All tests pass
  </acceptance_criteria>
  <done>
    `gigaevo plot comparison` and `gigaevo plot trajectory` produce PNG/PDF/SVG files.
    Matplotlib loaded lazily (only on command invocation). Tests pass with mocked data sources.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Export sub-group (csv + frontier) with tests</name>
  <files>
    gigaevo/cli/export.py,
    tests/cli/test_export_cmd.py
  </files>
  <read_first>
    gigaevo/cli/__init__.py,
    gigaevo/cli/run_resolver.py,
    tools/redis2pd.py
  </read_first>
  <behavior>
    CSV export:
    - export_csv_basic: `gigaevo export csv --run p@4:O --output-file /tmp/test.csv` produces CSV with program columns
    - export_csv_content: Output CSV has header row with expected columns (id, generation, fitness, state, etc.)

    Frontier export:
    - export_frontier_basic: `gigaevo export frontier --run p@4:O --output-file /tmp/frontier.csv` produces CSV with gen and best_val columns
    - export_frontier_content: Output has "gen" and "best_val" columns
    - export_frontier_sorted: Rows sorted by generation ascending

    General:
    - export_requires_output_file: Missing --output-file produces error
    - export_uses_resolver: Uses RunResolver for --run parsing
  </behavior>
  <action>
    1. Create `gigaevo/cli/export.py` as a Click sub-group:
       ```python
       import click

       @click.group()
       def export():
           """Export evolution data to CSV."""
           pass
       ```

    2. Add `csv` subcommand:
       - `@export.command("csv")`
       - Options: `--output-file` / `-o` (required, type=click.Path())
       - Implementation:
         a. Resolve run from RunResolver (first run)
         b. Build `RedisRunConfig` from RunSpec
         c. Call `asyncio.run(fetch_evolution_dataframe(config))` from `tools.utils`
         d. Serialize complex columns with `_serialize_complex_columns()` (reuse from tools/redis2pd.py or reimplement)
         e. Write to CSV with `df.to_csv(output_file, index=False)`
         f. Echo summary: `f"Full history: {len(df)} programs -> {output_file}"`

    3. Add `frontier` subcommand:
       - `@export.command("frontier")`
       - Options: `--output-file` / `-o` (required, type=click.Path()), `--metric` (default="fitness")
       - Implementation:
         a. Resolve run, fetch DataFrame
         b. Call `prepare_iteration_dataframe(df)`
         c. Group by iteration, take last frontier value per gen
         d. Write compact gen,best_val CSV
         e. Echo summary

    4. Write `tests/cli/test_export_cmd.py`:
       - Mock `fetch_evolution_dataframe` to return a DataFrame with id, generation, fitness columns
       - Mock `prepare_iteration_dataframe` for frontier test
       - Verify CSV files are created and have expected columns
       - Use `tmp_path` for output files
  </action>
  <verify>
    <automated>cd /mnt/virtual_ai0001071-04017_SR004-nfs1/CFS-SR008/workspace/mathemage/gigaevo-core-internal && PYTHONPATH=. /home/jovyan/.mlspace/envs/evo/bin/python3 -m pytest tests/cli/test_export_cmd.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep "click.group" gigaevo/cli/export.py` finds the sub-group definition
    - `grep "@export.command" gigaevo/cli/export.py` finds at least 2 subcommands (csv + frontier)
    - `grep -c "def test_" tests/cli/test_export_cmd.py` returns >= 5
    - `grep "output-file\|output_file" gigaevo/cli/export.py` confirms required output file option
    - `grep "to_csv" gigaevo/cli/export.py` confirms pandas CSV writing
    - All tests pass
  </acceptance_criteria>
  <done>
    `gigaevo export csv` exports full program history CSV (absorbs tools/redis2pd.py).
    `gigaevo export frontier` exports frontier-only CSV with gen,best_val columns.
    Tests pass with mocked DataFrames.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| --output-dir/--output-file -> filesystem write | User-provided path for plot/CSV output |
| Redis data -> plot/CSV | Evolution data rendered to files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | T (Tampering) | --output-dir path traversal | accept | Researcher provides output path on their own filesystem. No web-facing component. |
| T-04-08 | D (Denial of Service) | export csv with huge Redis | accept | Bounded by researcher's Redis DB size. CSV export is an explicit user action. |
| T-04-09 | I (Info Disclosure) | CSV export includes program code | accept | Same researcher who launched the experiment is exporting. No multi-tenancy. |
</threat_model>

<verification>
1. `PYTHONPATH=. python -m pytest tests/cli/test_plot_cmd.py tests/cli/test_export_cmd.py -x -v` -- all pass
2. `ruff check gigaevo/cli/plot_group.py gigaevo/cli/export.py tests/cli/` -- lint clean
3. `PYTHONPATH=. python -c "import gigaevo.cli.plot_group; import sys; assert 'matplotlib' not in sys.modules; print('lazy OK')"` -- matplotlib not imported at module level
</verification>

<success_criteria>
- `gigaevo plot comparison` produces PNG/PDF/SVG comparison curves
- `gigaevo plot trajectory` produces trajectory plots
- `gigaevo export csv` exports full evolution data (absorbs redis2pd.py)
- `gigaevo export frontier` exports frontier-only CSV
- Matplotlib loaded lazily
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-cli/04-03-SUMMARY.md`
</output>
