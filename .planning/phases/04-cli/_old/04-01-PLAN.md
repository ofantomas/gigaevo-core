---
phase: 04-cli
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gigaevo/cli/__init__.py
  - gigaevo/cli/output_formatter.py
  - gigaevo/cli/run_resolver.py
  - tests/cli/__init__.py
  - tests/cli/test_output_formatter.py
  - tests/cli/test_run_resolver.py
  - tests/cli/test_cli_group.py
autonomous: true
requirements:
  - CLI-01
  - CLI-02
  - CLI-04
  - CLI-06

must_haves:
  truths:
    - "gigaevo --help completes without importing matplotlib, redis, or heavy modules"
    - "Global flags --experiment, --run, --quiet, --verbose are available on the root group"
    - "OutputFormatter.render() produces table, json, csv, and markdown from the same data"
    - "When stdout is piped, format auto-switches to json unless --format is explicit"
    - "RunResolver.resolve() returns list[RunConfig] from either --experiment or --run"
    - "rich-click renders help with colored panels and grouped options"
  artifacts:
    - path: "gigaevo/cli/__init__.py"
      provides: "Click group with global flags, lazy subcommand loading"
      contains: "rich_click"
    - path: "gigaevo/cli/output_formatter.py"
      provides: "OutputFormatter class with table/json/csv/markdown"
      exports: ["OutputFormatter"]
    - path: "gigaevo/cli/run_resolver.py"
      provides: "RunResolver bridging CLI flags to monitoring.RunConfig"
      exports: ["RunResolver"]
    - path: "tests/cli/test_output_formatter.py"
      provides: "Tests for all 4 output formats + pipe detection"
    - path: "tests/cli/test_run_resolver.py"
      provides: "Tests for --experiment and --run resolution"
    - path: "tests/cli/test_cli_group.py"
      provides: "Tests for CLI group, help output, lazy imports"
  key_links:
    - from: "gigaevo/cli/run_resolver.py"
      to: "gigaevo/monitoring/run_spec.py"
      via: "RunSpec.parse()"
      pattern: "RunSpec\\.parse"
    - from: "gigaevo/cli/run_resolver.py"
      to: "gigaevo/monitoring/experiment_monitor.py"
      via: "RunConfig dataclass"
      pattern: "from gigaevo\\.monitoring.*import.*RunConfig"
    - from: "gigaevo/cli/__init__.py"
      to: "rich_click"
      via: "click = rich_click patching"
      pattern: "rich_click"
---

<objective>
Build the CLI foundation: Click group with rich-click help rendering, global flags (--experiment, --run, --quiet, --verbose, --format), OutputFormatter for structured output, and RunResolver that bridges CLI flags to the monitoring library's RunConfig. Replace the existing naive CLI skeleton in gigaevo/cli/__init__.py.

Purpose: Every subsequent CLI plan depends on this skeleton. OutputFormatter and RunResolver eliminate duplication across all subcommands.
Output: Working `gigaevo --help` with rich-click rendering, OutputFormatter tested with all 4 formats, RunResolver tested for both --experiment and --run modes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@gigaevo/monitoring/run_spec.py
@gigaevo/monitoring/experiment_monitor.py
@gigaevo/monitoring/snapshot.py
@gigaevo/monitoring/__init__.py
@gigaevo/cli/__init__.py

<interfaces>
<!-- Key types and contracts the executor needs from the monitoring library. -->

From gigaevo/monitoring/run_spec.py:
```python
@dataclass(frozen=True)
class RunSpec:
    prefix: str
    db: int
    label: str

    @classmethod
    def parse(cls, raw: str) -> RunSpec:
        """Parse 'prefix@db[:label]' into a RunSpec."""
```

From gigaevo/monitoring/experiment_monitor.py:
```python
@dataclass
class RunConfig:
    run_spec: RunSpec
    metric_names: list[str] = field(default_factory=lambda: ["fitness"])
    pid: int | None = None

class ExperimentMonitor:
    def __init__(self, redis_host, redis_port, redis_factory=None): ...
    def collect(self, runs: list[RunConfig]) -> list[RunSnapshot]: ...
```

From gigaevo/monitoring/snapshot.py:
```python
@dataclass(frozen=True)
class RunSnapshot:
    run_spec: RunSpec
    generation: int | None = None
    metrics: dict[str, float | None] = field(default_factory=dict)
    total_programs: int | None = None
    valid_programs: int | None = None
    running_programs: int | None = None
    queued_programs: int | None = None
    done_programs: int | None = None
    validator_mean_s: float | None = None
    validator_max_s: float | None = None
    total_keys: int | None = None
    pid: int | None = None
    pid_alive: bool | None = None
    error: str | None = None

    @property
    def invalid_rate(self) -> float | None: ...
```

From gigaevo/monitoring/manifest_schema.py (loaded by RunResolver for --experiment mode):
```python
class ExperimentManifest(BaseModel):
    # Loaded via load_manifest() from tools/experiment/manifest.py
    # Has .runs list with prefix, db, label, problem_name, pid fields
```

From pyproject.toml:
```toml
[project.scripts]
gigaevo = "gigaevo.cli:main"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RunResolver + OutputFormatter + tests (RED then GREEN)</name>
  <files>
    gigaevo/cli/run_resolver.py,
    gigaevo/cli/output_formatter.py,
    tests/cli/__init__.py,
    tests/cli/test_run_resolver.py,
    tests/cli/test_output_formatter.py
  </files>
  <read_first>
    gigaevo/monitoring/run_spec.py,
    gigaevo/monitoring/experiment_monitor.py,
    gigaevo/monitoring/snapshot.py,
    gigaevo/cli/__init__.py,
    tests/monitoring/test_redis_queries.py
  </read_first>
  <behavior>
    RunResolver tests:
    - resolve_from_run_flags: Given ["prefix@4:O"], returns [RunConfig(RunSpec("prefix", 4, "O"), ["fitness"])]
    - resolve_from_run_flags_multiple: Given ["p@1:A", "p@2:B"], returns 2 RunConfigs
    - resolve_from_experiment_flag: Given "hover/test" with mock manifest, returns RunConfigs with correct metric_names and pids from manifest
    - resolve_raises_if_neither: If experiment=None and runs=[], raises click.UsageError
    - resolve_prefers_experiment: If both --experiment and --run are given, raises click.UsageError("Use --experiment or --run, not both")

    OutputFormatter tests:
    - format_table: Given list of dicts, produces Rich table with correct columns and aligned values
    - format_json: Given list of dicts, produces valid JSON array (json.loads succeeds)
    - format_csv: Given list of dicts, produces CSV with header row + data rows
    - format_markdown: Given list of dicts, produces markdown table with | separators
    - auto_detect_pipe: When sys.stdout.isatty() returns False and no explicit --format, defaults to "json"
    - auto_detect_terminal: When sys.stdout.isatty() returns True and no explicit --format, defaults to "table"
    - explicit_format_overrides_pipe: When --format=table and stdout is piped, still uses "table"
  </behavior>
  <action>
    1. Create `tests/cli/__init__.py` (empty).

    2. Create `gigaevo/cli/run_resolver.py`:
       - Class `RunResolver` with method `resolve(experiment: str | None, runs: list[str], redis_host: str, redis_port: int) -> list[RunConfig]`
       - If both experiment and runs are non-empty, raise `click.UsageError("Use --experiment or --run, not both")`
       - If neither, raise `click.UsageError("Provide --experiment or at least one --run")`
       - For --run mode: parse each string with `RunSpec.parse()`, wrap in `RunConfig(run_spec=spec)`
       - For --experiment mode: lazy-import `tools.experiment.manifest.load_manifest`, iterate `manifest.runs`, build `RunConfig` with metric_names from `problems/{problem_name}/metrics.yaml` and pid from manifest
       - Helper `_load_metric_names(problem_name: str) -> list[str]` reads `problems/{problem_name}/metrics.yaml` and returns metric names (primary first, excluding is_valid), defaulting to `["fitness"]`

    3. Create `gigaevo/cli/output_formatter.py`:
       - Class `OutputFormatter` with `__init__(self, format_name: str | None, quiet: bool = False)`
       - Property `effective_format` that resolves: explicit format_name > pipe detection (json if not tty) > "table"
       - Method `render(self, rows: list[dict], columns: list[str] | None = None, title: str | None = None) -> str`
         - "table": Use `rich.table.Table` rendered to string via `rich.console.Console(file=StringIO())`
         - "json": `json.dumps(rows, indent=2, default=str)`
         - "csv": `csv.DictWriter` to `StringIO` with `columns` as fieldnames
         - "markdown": Pipe-separated table with header and separator row
       - Method `echo(self, rows: list[dict], columns: list[str] | None = None, title: str | None = None)` that calls render() and click.echo()
       - If quiet=True, echo() is a no-op

    4. Write tests in `tests/cli/test_run_resolver.py` and `tests/cli/test_output_formatter.py` with the behaviors listed above. Use `unittest.mock.patch` for `sys.stdout.isatty`, and for the experiment resolution test, mock `tools.experiment.manifest.load_manifest`.
  </action>
  <verify>
    <automated>cd /mnt/virtual_ai0001071-04017_SR004-nfs1/CFS-SR008/workspace/mathemage/gigaevo-core-internal && PYTHONPATH=. /home/jovyan/.mlspace/envs/evo/bin/python3 -m pytest tests/cli/test_run_resolver.py tests/cli/test_output_formatter.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" tests/cli/test_run_resolver.py` returns >= 5
    - `grep -c "def test_" tests/cli/test_output_formatter.py` returns >= 7
    - `grep "RunSpec.parse" gigaevo/cli/run_resolver.py` finds at least 1 match (uses monitoring lib, not reimplements)
    - `grep "from gigaevo.monitoring" gigaevo/cli/run_resolver.py` finds RunConfig and RunSpec imports
    - `grep "class OutputFormatter" gigaevo/cli/output_formatter.py` finds the class
    - `grep "rich.table" gigaevo/cli/output_formatter.py` confirms Rich is used for table format
    - All tests pass
  </acceptance_criteria>
  <done>
    RunResolver resolves both --experiment and --run flags to list[RunConfig] using monitoring library.
    OutputFormatter renders table/json/csv/markdown from same data. Pipe auto-detection works.
    All tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CLI group with rich-click, global flags, lazy imports + tests</name>
  <files>
    gigaevo/cli/__init__.py,
    tests/cli/test_cli_group.py
  </files>
  <read_first>
    gigaevo/cli/__init__.py,
    gigaevo/cli/run_resolver.py,
    gigaevo/cli/output_formatter.py,
    pyproject.toml
  </read_first>
  <behavior>
    - help_output: `gigaevo --help` exits 0 and output contains "GigaEvo" and "status" and "plot"
    - help_fast: `gigaevo --help` does NOT import matplotlib (verified by checking sys.modules post-import)
    - global_flags_exist: --experiment, --run, --format, --quiet, --verbose appear in help text
    - format_choices: --format accepts table, json, csv, markdown
    - subcommand_groups: "status", "trajectory", "top", "logs", "plot", "flush", "watchdog" appear in help
    - context_passes_formatter: Click context obj contains OutputFormatter and resolved format
  </behavior>
  <action>
    1. Install rich-click: `pip install rich-click` (the executor should run this). Add `"rich-click>=1.8"` to pyproject.toml dependencies list (after "click" line).

    2. Rewrite `gigaevo/cli/__init__.py`:
       - At module top: `import rich_click as click` (this patches Click to use Rich rendering). Fall back to `import click` if rich-click not installed.
       - Define `@click.group()` named `main` with docstring "GigaEvo CLI -- unified experiment monitoring and analysis."
       - Add `@click.pass_context` and global options:
         - `--experiment` / `-e`: `click.Option`, type=str, default=None, help="Experiment name (task/name). Reads experiment.yaml for run discovery."
         - `--run` / `-r`: `click.Option`, type=str, multiple=True, help="Run spec prefix@db[:label]. Repeatable."
         - `--format` / `-f`: `click.Choice(["table", "json", "csv", "markdown"])`, default=None
         - `--quiet` / `-q`: is_flag, default=False
         - `--verbose` / `-v`: is_flag, default=False
         - `--redis-host`: default="localhost"
         - `--redis-port`: type=int, default=6379
       - In the group callback, store `OutputFormatter(format_name=format, quiet=quiet)` and raw flag values on `ctx.ensure_object(dict)` so subcommands can access them via `@click.pass_context`.
       - Use LAZY subcommand registration to avoid importing heavy modules at startup. Use `click.Group` with `lazy_subcommands` pattern: define a custom `LazyGroup(click.Group)` that imports subcommand modules only when invoked.
         - Alternative simpler approach: register subcommands as stubs via `@main.command(cls=LazyCommand)` or just use `add_command` with deferred imports inside each subcommand function. The simplest correct approach: define all subcommand functions in separate modules, import them lazily using a dict mapping name->module_path, override `Group.get_command()` to do the import.
       - Subcommands to register (as stubs, implementation in later plans):
         - `status` -> `gigaevo.cli.status:status`
         - `trajectory` -> `gigaevo.cli.trajectory:trajectory`
         - `top` -> `gigaevo.cli.top:top`
         - `logs` -> `gigaevo.cli.logs:logs`
         - `plot` -> `gigaevo.cli.plot_group:plot` (sub-group with comparison, trajectory sub-subcommands)
         - `flush` -> `gigaevo.cli.flush:flush`
         - `watchdog` -> `gigaevo.cli.watchdog:watchdog`
         - `export` -> `gigaevo.cli.export:export`
       - For subcommands not yet implemented (trajectory, top, logs, flush, watchdog, export), create placeholder stub modules that raise `click.ClickException("Not yet implemented")` so the group is complete.
       - Remove the old eager imports of analyze, collect, plot, status at bottom of file.

    3. Create placeholder stub modules (one-liners that register a click command that raises NotImplementedError):
       - `gigaevo/cli/trajectory.py`
       - `gigaevo/cli/top.py`
       - `gigaevo/cli/logs.py`
       - `gigaevo/cli/flush.py`
       - `gigaevo/cli/watchdog_cmd.py`
       - `gigaevo/cli/export.py`
       - `gigaevo/cli/plot_group.py` (a click.Group with `comparison` and `trajectory` stubs)
       Each stub: `import click` + a `@click.command()` function that does `click.echo("Not yet implemented")`.

    4. Write tests in `tests/cli/test_cli_group.py`:
       - Use `click.testing.CliRunner` to invoke `main` with `--help`
       - Verify exit code 0
       - Verify output contains expected subcommand names
       - Test that importing `gigaevo.cli` does NOT pull in matplotlib: `import gigaevo.cli; assert "matplotlib" not in sys.modules`
       - Test context object: invoke a mock subcommand, verify ctx.obj has "formatter" and "experiment" keys
  </action>
  <verify>
    <automated>cd /mnt/virtual_ai0001071-04017_SR004-nfs1/CFS-SR008/workspace/mathemage/gigaevo-core-internal && PYTHONPATH=. /home/jovyan/.mlspace/envs/evo/bin/python3 -m pytest tests/cli/test_cli_group.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep "rich_click" gigaevo/cli/__init__.py` finds at least 1 match
    - `grep -c "def test_" tests/cli/test_cli_group.py` returns >= 4
    - `grep "LazyGroup\|lazy\|get_command" gigaevo/cli/__init__.py` confirms lazy loading pattern
    - `grep "OutputFormatter" gigaevo/cli/__init__.py` confirms formatter is created from global flags
    - `grep "experiment.*run.*format.*quiet.*verbose" gigaevo/cli/__init__.py` or individual greps confirm all 5 global flags exist
    - Running `PYTHONPATH=. python -c "from gigaevo.cli import main; import sys; assert 'matplotlib' not in sys.modules; print('OK')"` prints OK
    - All test_cli_group.py tests pass
  </acceptance_criteria>
  <done>
    CLI group uses rich-click, has all global flags, loads subcommands lazily (no matplotlib at import time).
    Stub subcommands registered for all planned commands. Old analyze/collect/plot/status imports removed.
    Context passes OutputFormatter to subcommands. All tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args -> RunSpec.parse | User-provided --run strings parsed into structured RunSpec |
| CLI args -> Redis connection | --redis-host/--redis-port used to connect to Redis |
| experiment.yaml -> RunConfig | Manifest file read from disk, parsed into config objects |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T (Tampering) | experiment.yaml | accept | File is local, researcher-controlled. Pydantic validation from Phase 1 catches malformed data. |
| T-04-02 | I (Info Disclosure) | --format json | accept | CLI outputs experiment data to stdout. Researcher's terminal, no multi-tenancy concern. |
| T-04-03 | D (Denial of Service) | OutputFormatter with large datasets | mitigate | OutputFormatter truncates table rows to 500 by default; json/csv emit all. |
</threat_model>

<verification>
1. `PYTHONPATH=. python -m pytest tests/cli/ -x -v` -- all tests pass
2. `PYTHONPATH=. python -c "from gigaevo.cli import main; import sys; assert 'matplotlib' not in sys.modules"` -- lazy imports confirmed
3. `PYTHONPATH=. python -c "from click.testing import CliRunner; from gigaevo.cli import main; r = CliRunner().invoke(main, ['--help']); assert r.exit_code == 0; assert 'status' in r.output"` -- help works
4. `ruff check gigaevo/cli/ tests/cli/` -- lint clean
</verification>

<success_criteria>
- CLI group `gigaevo --help` works with rich-click rendering, lists all subcommands
- Global flags --experiment, --run, --format, --quiet, --verbose available on root group
- OutputFormatter produces 4 formats from same data, auto-detects pipe -> json
- RunResolver bridges CLI flags to monitoring RunConfig using RunSpec.parse()
- No matplotlib import at CLI startup (lazy loading works)
- All tests in tests/cli/ pass
- Lint clean
</success_criteria>

<output>
After completion, create `.planning/phases/04-cli/04-01-SUMMARY.md`
</output>
