---
phase: 04-cli
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - experiments/_template/04_issues_log.md
  - experiments/PATTERNS.md
autonomous: true
requirements:
  - D-07
  - D-08
must_haves:
  truths:
    - "The issues log template distinguishes EVENT entries from ISSUE entries"
    - "PATTERNS.md contains a Known Failures section with structured entries"
    - "Known Failure entries include ID, Trigger, Symptoms, Fix, Status, Affected Types, and Source fields"
    - "Real known failures from heilbron/asymmetric-iterations are populated in the table"
  artifacts:
    - path: "experiments/_template/04_issues_log.md"
      provides: "Updated template with EVENT vs ISSUE format guidance"
      contains: "[EVENT"
    - path: "experiments/PATTERNS.md"
      provides: "Known Failures section with structured entries"
      contains: "## Known Failures"
  key_links:
    - from: "experiments/PATTERNS.md Known Failures"
      to: "experiment-implement Step 4 pattern research"
      via: "Claude reads PATTERNS.md during implementation"
      pattern: "Known Failures"
---

<objective>
Establish the two foundation knowledge stores that all subsequent plans reference: the updated issues log template (with EVENT vs ISSUE distinction per D-07) and the Known Failures section in PATTERNS.md (per D-08).

Purpose: Plans 02-04 add event-capture snippets and pattern-promotion steps that write to these formats. The formats must be established first so all downstream plans reference a concrete, existing structure.

Output: Updated `experiments/_template/04_issues_log.md` and `experiments/PATTERNS.md` with new sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cli/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update issues log template with EVENT vs ISSUE format guidance</name>
  <files>experiments/_template/04_issues_log.md</files>
  <read_first>
    - experiments/_template/04_issues_log.md
    - experiments/heilbron/asymmetric-iterations/04_issues_log.md
  </read_first>
  <action>
Modify `experiments/_template/04_issues_log.md` to add EVENT vs ISSUE format guidance (per D-07, addressing Pitfall 4 from research). Keep all existing content intact. Add a new section after the existing "## Format" section.

Add the following content after the existing format section (before the `---` separator and HTML comment):

```markdown
## Entry Types

### Events (auto-captured by lifecycle skills)

Brief entries auto-appended by `/experiment-launch`, `/experiment-restart`, `/experiment-checkpoint`, and `/experiment-diagnose`. One-line summary with structured metadata. Format:

```
### [EVENT <ISO-timestamp>] -- <one-line description>

- **When**: <ISO-timestamp>
- **What**: <one-line description>
- **Category**: launch | restart | checkpoint | watchdog | diagnose
- **Impact**: <brief impact or "automated capture">
```

### Issues (manual or escalated entries)

Detailed entries for things that went wrong and required intervention. Use the full format from above (When, What, Category, Impact, Root cause, Fix applied, Systemic fix needed).

```
### <timestamp> -- <description>

- **When**: <timestamp or phase>
- **What**: <brief description>
- **Category**: run crash | tool bug | config mistake | infra issue | skill bug | watchdog | git | other
- **Impact**: <how it affected the experiment>
- **Root cause**: <why it happened>
- **Fix applied**: <what was done>
- **Systemic fix needed**: YES/NO + description
```
```

This distinguishes automated event capture (brief, always logged) from manual issue entries (detailed, logged when something breaks), per D-07 and research Pitfall 4 (preventing auto-capture noise from drowning signal).
  </action>
  <verify>
    <automated>grep -c "\[EVENT" experiments/_template/04_issues_log.md</automated>
  </verify>
  <acceptance_criteria>
    - experiments/_template/04_issues_log.md contains the string "## Entry Types"
    - experiments/_template/04_issues_log.md contains the string "[EVENT"
    - experiments/_template/04_issues_log.md contains the string "### Events (auto-captured by lifecycle skills)"
    - experiments/_template/04_issues_log.md contains the string "### Issues (manual or escalated entries)"
    - experiments/_template/04_issues_log.md still contains the original "## Format" section header
    - experiments/_template/04_issues_log.md still contains "<!-- Add entries below, newest first -->"
  </acceptance_criteria>
  <done>Template file contains EVENT vs ISSUE format guidance with clear examples of both entry types</done>
</task>

<task type="auto">
  <name>Task 2: Add Known Failures section to PATTERNS.md with real entries</name>
  <files>experiments/PATTERNS.md</files>
  <read_first>
    - experiments/PATTERNS.md
    - experiments/heilbron/asymmetric-iterations/04_issues_log.md
    - .planning/phases/04-cli/04-RESEARCH.md
  </read_first>
  <action>
Add a new "## Known Failures" section to `experiments/PATTERNS.md` (per D-08). Insert it AFTER the existing "## GigaEvo Platform Failure Modes" section and BEFORE the existing "## Recurring Design Flaws" section.

The section header and introduction text:

```markdown
## Known Failures

Structured entries for `/experiment-implement` and `/experiment-launch` to check proactively. Each entry describes a specific failure mode with enough detail to detect and prevent it. Updated by `/post-experiment-fixes` and `/experiment-closeout`.

| ID | Trigger Condition | Symptoms | Root Cause | Fix | Status | Affected Types | Source |
|----|-------------------|----------|------------|-----|--------|----------------|--------|
| KF-01 | adversarial pipeline + missing `evolution=steady_state` override | All runs deadlock at gen 0 | MainRunSyncHook waits on `total_generations` incremented after hook call; circular wait in SteadyState | Override `pre_step_hook` to `ProgressBasedSyncHook`; or add `evolution=steady_state` to extra_overrides | FIXED (e69021c0) | adversarial | heilbron/asymmetric-iterations |
| KF-02 | extra_overrides containing `${}` Hydra interpolation refs | launch.sh expands `${...}` as empty shell variable | `generate_launch.py` does not quote Hydra interpolation refs | Single-quote all `${}` refs in extra_overrides in `generate_launch.py` | FIXED (fdd3dae1) | all | heilbron/asymmetric-iterations |
| KF-03 | Missing `population_role` in adversarial_asymmetric runs | Pipeline cannot differentiate G vs D roles; wrong stages run | `experiment.yaml` missing per-run `population_role=constructor/improver` overrides | Add `population_role=constructor` or `population_role=improver` per run in experiment.yaml | ACTIVE | adversarial_asymmetric | heilbron/asymmetric-iterations |
| KF-04 | CompositionInjectionHook programs missing `iteration` field | MetricsTracker crashes on KeyError, kills entire tracker async task; frontier never updates | Programs created without `iteration` in metadata | Promoted `iteration` to typed Program field with `default=0` | FIXED | adversarial with composition | heilbron/asymmetric-iterations |
| KF-05 | `min_delta=1` in ProgressBasedSyncHook with asymmetric runs | D runs 2.5x faster than G; massive desync | D processes 1 program and unblocks immediately; no epoch-level sync | Set `min_delta` to `max_mutations_per_generation` (e.g., 8) for proper epoch-level sync | FIXED | adversarial_asymmetric | heilbron/asymmetric-iterations |
```

Also update the footer of PATTERNS.md to mention Known Failures are updated by `/post-experiment-fixes`:

Change the last line from:
```
*Updated by `/experiment-closeout` and `/experiment-retrospective`. All agents read; no agent duplicates in its own memory.*
```
to:
```
*Updated by `/experiment-closeout`, `/experiment-retrospective`, and `/post-experiment-fixes`. All agents read; no agent duplicates in its own memory.*
```
  </action>
  <verify>
    <automated>grep -c "Known Failures" experiments/PATTERNS.md && grep -c "KF-0" experiments/PATTERNS.md</automated>
  </verify>
  <acceptance_criteria>
    - experiments/PATTERNS.md contains "## Known Failures"
    - experiments/PATTERNS.md contains "KF-01" through "KF-05" (5 entries)
    - experiments/PATTERNS.md contains "Structured entries for `/experiment-implement`"
    - experiments/PATTERNS.md contains "post-experiment-fixes" in the footer line
    - experiments/PATTERNS.md still contains all pre-existing sections: "## Confirmed Patterns", "## Refuted Hypotheses", "## Suggestive Signals", "## GigaEvo Platform Failure Modes", "## Recurring Design Flaws", "## Protocol Gaps", "## Open Questions"
    - The Known Failures section appears AFTER "## GigaEvo Platform Failure Modes" and BEFORE "## Recurring Design Flaws"
  </acceptance_criteria>
  <done>PATTERNS.md has a Known Failures section with 5 real entries from heilbron/asymmetric-iterations issues log, and footer references /post-experiment-fixes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan modifies only Markdown template files, not executable code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T (Tampering) | PATTERNS.md | accept | Markdown file under git version control; no executable code; tampering is self-evident in diff review |
| T-04-02 | I (Information Disclosure) | 04_issues_log.md template | accept | Template contains format guidance only, no secrets; actual issues logs are per-experiment and reviewed at closeout |
</threat_model>

<verification>
1. `grep -c "\[EVENT" experiments/_template/04_issues_log.md` returns >= 1
2. `grep -c "## Known Failures" experiments/PATTERNS.md` returns 1
3. `grep -c "KF-0" experiments/PATTERNS.md` returns >= 5
4. All pre-existing PATTERNS.md sections still present
</verification>

<success_criteria>
- Issues log template has clear EVENT vs ISSUE format guidance
- PATTERNS.md has Known Failures section with 5 real entries from heilbron/asymmetric-iterations
- No existing content removed from either file
</success_criteria>

<output>
After completion, create `.planning/phases/04-cli/04-01-SUMMARY.md`
</output>
