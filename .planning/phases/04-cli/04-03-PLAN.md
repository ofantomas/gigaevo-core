---
phase: 04-cli
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - .claude/skills/experiment-restart/SKILL.md
  - .claude/skills/experiment-checkpoint/SKILL.md
  - .claude/skills/experiment-diagnose/SKILL.md
autonomous: true
requirements:
  - D-06
  - D-07
must_haves:
  truths:
    - "experiment-restart auto-appends an EVENT entry to 04_issues_log.md after researcher confirms restart"
    - "experiment-checkpoint auto-appends an EVENT entry after recording the checkpoint"
    - "experiment-diagnose auto-appends an EVENT entry for CRITICAL or MAJOR findings"
    - "All event entries use the [EVENT <timestamp>] format established in the template"
    - "Event entries include When, What, Category, and Impact fields"
  artifacts:
    - path: ".claude/skills/experiment-restart/SKILL.md"
      provides: "Event auto-capture for restart events"
      contains: "[EVENT"
    - path: ".claude/skills/experiment-checkpoint/SKILL.md"
      provides: "Event auto-capture for checkpoint events"
      contains: "[EVENT"
    - path: ".claude/skills/experiment-diagnose/SKILL.md"
      provides: "Event auto-capture for diagnose findings"
      contains: "[EVENT"
  key_links:
    - from: "lifecycle skill event capture snippets"
      to: "experiments/<task>/<name>/04_issues_log.md"
      via: "bash cat >> append"
      pattern: "cat >> .*04_issues_log"
---

<objective>
Add event auto-capture bash snippets to experiment-restart, experiment-checkpoint, and experiment-diagnose skills so that lifecycle events are automatically logged to 04_issues_log.md per D-06 and D-07.

Purpose: Creates a comprehensive event trail for each experiment without manual effort. Events are brief one-line entries (not detailed issue entries) per the EVENT format established in Plan 01.

Output: Modified SKILL.md files for restart, checkpoint, and diagnose with auto-capture snippets at appropriate trigger points.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cli/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add event auto-capture to experiment-restart SKILL.md</name>
  <files>.claude/skills/experiment-restart/SKILL.md</files>
  <read_first>
    - .claude/skills/experiment-restart/SKILL.md
    - experiments/_template/04_issues_log.md
    - .planning/phases/04-cli/04-RESEARCH.md
  </read_first>
  <action>
Modify `.claude/skills/experiment-restart/SKILL.md` to add event auto-capture at two points. Do NOT change any existing step logic, numbering, or gates.

**Point 1: After Step 2 (Confirm with researcher) -- log restart initiation event**

Insert a new bash block at the END of Step 2, after the existing confirmation text ("Do NOT proceed without explicit confirmation."). Add the following:

```markdown
After confirmation, log the restart event:

```bash
EXP='$ARGUMENTS'
PROJ="$(git rev-parse --show-toplevel)"
LOG="$PROJ/experiments/$EXP/04_issues_log.md"
[ ! -f "$LOG" ] && cp "$PROJ/experiments/_template/04_issues_log.md" "$LOG"
GEN_INFO=$(gigaevo -e "$EXP" manifest get runs --format json | \
  $GIGAEVO_PYTHON -c "
import sys, json, redis
runs = json.load(sys.stdin)
parts = []
for run in runs:
    db = int(run.get('DB') or run.get('db'))
    prefix = run.get('Prefix') or run.get('prefix')
    label = run.get('Label') or run.get('label')
    r = redis.Redis(host='localhost', port=6379, db=db)
    gen = int(r.hget(f'{prefix}:run_state', 'engine:total_generations') or 0)
    parts.append(f'{label}=gen{gen}')
print(' '.join(parts))
")

cat >> "$LOG" << ENTRY

### [EVENT $(date -u +%Y-%m-%dT%H:%M:%SZ)] -- Experiment restart initiated

- **When**: $(date -u +%Y-%m-%dT%H:%M:%SZ)
- **What**: Full experiment restart. Progress at restart: $GEN_INFO
- **Category**: restart
- **Impact**: All run progress destroyed. Redis DBs flushed.
ENTRY
echo "Restart event logged to 04_issues_log.md"
```
```

**Point 2: After Step 4 (Flush Redis DBs) -- log flush completion event**

Insert a new bash block at the END of Step 4, after the existing flush commands. Add the following:

```markdown
Log the flush completion:

```bash
EXP='$ARGUMENTS'
PROJ="$(git rev-parse --show-toplevel)"
LOG="$PROJ/experiments/$EXP/04_issues_log.md"
cat >> "$LOG" << ENTRY

### [EVENT $(date -u +%Y-%m-%dT%H:%M:%SZ)] -- Redis flush completed

- **When**: $(date -u +%Y-%m-%dT%H:%M:%SZ)
- **What**: Redis DBs flushed for $EXP. Status reset to implemented.
- **Category**: restart
- **Impact**: Data destroyed. Ready for re-launch.
ENTRY
```
```
  </action>
  <verify>
    <automated>grep -c "\[EVENT" .claude/skills/experiment-restart/SKILL.md && grep -c "04_issues_log" .claude/skills/experiment-restart/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/experiment-restart/SKILL.md contains "[EVENT" (at least 2 occurrences -- restart initiation and flush completion)
    - .claude/skills/experiment-restart/SKILL.md contains "cat >> " followed by "04_issues_log.md" (append operation)
    - .claude/skills/experiment-restart/SKILL.md contains "Experiment restart initiated" (restart event description)
    - .claude/skills/experiment-restart/SKILL.md contains "Redis flush completed" (flush event description)
    - .claude/skills/experiment-restart/SKILL.md contains "Category**: restart" (event category)
    - .claude/skills/experiment-restart/SKILL.md still contains "## Step 0 -- Cancel anomaly detector cron" (existing step preserved)
    - .claude/skills/experiment-restart/SKILL.md still contains "## Step 7 -- Dispatch to /experiment-launch" (last step preserved)
    - .claude/skills/experiment-restart/SKILL.md still contains "manifest gate running" in Step 1 (gate preserved)
  </acceptance_criteria>
  <done>experiment-restart SKILL.md has two auto-capture snippets: restart initiation (Step 2) and flush completion (Step 4)</done>
</task>

<task type="auto">
  <name>Task 2: Add event auto-capture to experiment-checkpoint and experiment-diagnose</name>
  <files>.claude/skills/experiment-checkpoint/SKILL.md, .claude/skills/experiment-diagnose/SKILL.md</files>
  <read_first>
    - .claude/skills/experiment-checkpoint/SKILL.md
    - .claude/skills/experiment-diagnose/SKILL.md
    - experiments/_template/04_issues_log.md
    - .planning/phases/04-cli/04-RESEARCH.md
  </read_first>
  <action>
Modify both experiment-checkpoint and experiment-diagnose SKILL.md files to add event auto-capture. Do NOT change any existing step logic, numbering, or gates.

**experiment-checkpoint/SKILL.md -- Add checkpoint event at Step 8 (Record checkpoint in manifest)**

Insert a bash block at the END of Step 8, after the existing Python script that writes the checkpoint to experiment.yaml. Add:

```markdown
Log the checkpoint event:

```bash
EXP='$ARGUMENTS'
PROJ="$(git rev-parse --show-toplevel)"
LOG="$PROJ/experiments/$EXP/04_issues_log.md"
[ ! -f "$LOG" ] && cp "$PROJ/experiments/_template/04_issues_log.md" "$LOG"
AVG_GEN=$(gigaevo -e "$EXP" manifest get runs --format json | \
  $GIGAEVO_PYTHON -c "
import sys, json, redis
runs = json.load(sys.stdin)
gens = []
for run in runs:
    db = int(run.get('DB') or run.get('db'))
    prefix = run.get('Prefix') or run.get('prefix')
    r = redis.Redis(host='localhost', port=6379, db=db)
    gen = int(r.hget(f'{prefix}:run_state', 'engine:total_generations') or 0)
    gens.append(gen)
print(int(sum(gens)/max(len(gens),1)))
")

cat >> "$LOG" << ENTRY

### [EVENT $(date -u +%Y-%m-%dT%H:%M:%SZ)] -- Checkpoint recorded at gen ~$AVG_GEN

- **When**: $(date -u +%Y-%m-%dT%H:%M:%SZ)
- **What**: Checkpoint recorded. Average generation: ~$AVG_GEN.
- **Category**: checkpoint
- **Impact**: Automated capture.
ENTRY
```
```

**experiment-checkpoint/SKILL.md -- Add anomaly event at Step 2a (Stopping rule compliance)**

Insert a bash block at the END of Step 2a (after the stopping rule check Python script), to log when a stopping rule violation is detected. Add:

```markdown
If a stopping rule violation was detected above, log it:

```bash
EXP='$ARGUMENTS'
PROJ="$(git rev-parse --show-toplevel)"
LOG="$PROJ/experiments/$EXP/04_issues_log.md"
[ ! -f "$LOG" ] && cp "$PROJ/experiments/_template/04_issues_log.md" "$LOG"
# Only append if the check above printed WARNING (manual check by Claude -- append only if stopping rule was triggered)
```

Note: Claude should check the output of the Step 2a script. If any WARNING about stopping rule threshold was printed, append an EVENT entry:

```
### [EVENT <timestamp>] -- Stopping rule threshold exceeded

- **When**: <timestamp>
- **What**: Run <label> invalidity <rate> exceeds pre-registered stop threshold <threshold>
- **Category**: checkpoint
- **Impact**: Stopping rule may have been triggered. Review required.
```
```

**experiment-diagnose/SKILL.md -- Add diagnose event at "## Issues log" section**

The diagnose skill already has an "## Issues log" section at the end that instructs Claude to append to 04_issues_log.md for CRITICAL/MAJOR findings. Enhance this section to also log a brief EVENT entry for all diagnose invocations (not just when issues are found). Modify the existing "## Issues log" section to:

```markdown
## Issues log

After diagnosing, auto-capture the diagnosis as an event in the issues log:

```bash
EXP='$ARGUMENTS'
PROJ="$(git rev-parse --show-toplevel)"
LOG="$PROJ/experiments/$EXP/04_issues_log.md"
[ ! -f "$LOG" ] && cp "$PROJ/experiments/_template/04_issues_log.md" "$LOG"

cat >> "$LOG" << ENTRY

### [EVENT $(date -u +%Y-%m-%dT%H:%M:%SZ)] -- Diagnose completed: <VERDICT>

- **When**: $(date -u +%Y-%m-%dT%H:%M:%SZ)
- **What**: Diagnosis completed. Verdict: <HEALTHY|MINOR|MAJOR|CRITICAL>. <one-line summary of findings or "No issues found">
- **Category**: diagnose
- **Impact**: <brief impact or "No issues">
ENTRY
```

Replace `<VERDICT>` and other placeholders with the actual diagnosis outcome from Step 8.

If any CRITICAL or MAJOR issues were found (or any manual fix was applied), ALSO append a detailed ISSUE entry (not just an EVENT) with the full format: When, What, Category, Impact, Root cause, Fix applied, Systemic fix needed. This builds a per-experiment record for post-mortem reflection.
```
  </action>
  <verify>
    <automated>grep -c "\[EVENT" .claude/skills/experiment-checkpoint/SKILL.md && grep -c "\[EVENT" .claude/skills/experiment-diagnose/SKILL.md && grep -c "04_issues_log" .claude/skills/experiment-checkpoint/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/experiment-checkpoint/SKILL.md contains "[EVENT" (at least 2 occurrences -- checkpoint recorded and stopping rule)
    - .claude/skills/experiment-checkpoint/SKILL.md contains "Checkpoint recorded at gen" (checkpoint event description)
    - .claude/skills/experiment-checkpoint/SKILL.md contains "Category**: checkpoint" (event category)
    - .claude/skills/experiment-checkpoint/SKILL.md still contains "## Step 0 -- Gate check" (preserved)
    - .claude/skills/experiment-checkpoint/SKILL.md still contains "## Step 9 -- Update PR" (preserved)
    - .claude/skills/experiment-diagnose/SKILL.md contains "[EVENT" (at least 1 occurrence)
    - .claude/skills/experiment-diagnose/SKILL.md contains "Diagnose completed" (diagnose event description)
    - .claude/skills/experiment-diagnose/SKILL.md contains "Category**: diagnose" (event category)
    - .claude/skills/experiment-diagnose/SKILL.md still contains "## Step 0 -- Understand the experiment hypothesis" (preserved)
    - .claude/skills/experiment-diagnose/SKILL.md still contains "## Step 8 -- Report" (preserved)
    - Both files still contain "cat >> " followed by "04_issues_log" (append operation)
  </acceptance_criteria>
  <done>experiment-checkpoint has auto-capture at checkpoint recording and stopping rule violation; experiment-diagnose has auto-capture for all diagnose completions with escalation to full ISSUE entry for CRITICAL/MAJOR findings</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan modifies only Markdown skill files, not executable code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | T (Tampering) | SKILL.md files | accept | Markdown under git; no executable code; auto-capture is bash snippets that Claude follows |
| T-04-06 | I (Information Disclosure) | 04_issues_log.md entries | accept | Issues logs are per-experiment, reviewed at closeout; no secrets in event entries |
</threat_model>

<verification>
1. `grep -c "\[EVENT" .claude/skills/experiment-restart/SKILL.md` returns >= 2
2. `grep -c "\[EVENT" .claude/skills/experiment-checkpoint/SKILL.md` returns >= 2
3. `grep -c "\[EVENT" .claude/skills/experiment-diagnose/SKILL.md` returns >= 1
4. All three skills preserve existing step numbering and gates
</verification>

<success_criteria>
- experiment-restart has 2 event capture points (restart initiation, flush completion)
- experiment-checkpoint has 2 event capture points (checkpoint recorded, stopping rule violation)
- experiment-diagnose has event capture for all diagnose completions
- All events use the [EVENT <timestamp>] format from the template
- No existing step logic, numbering, or gates were changed
</success_criteria>

<output>
After completion, create `.planning/phases/04-cli/04-03-SUMMARY.md`
</output>
