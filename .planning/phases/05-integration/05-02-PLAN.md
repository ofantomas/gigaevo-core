---
phase: 05-integration
plan: 02
type: execute
wave: 1
depends_on: ["04-04"]
files_modified:
  - gigaevo/cli/lifecycle.py
  - tests/cli/test_lifecycle_cmd.py
autonomous: true
requirements:
  - EXT-03
  - EXT-04
  - EXT-05
---

<objective>
Implement composite lifecycle subcommands: `gigaevo launch`, `gigaevo closeout`,
`gigaevo restart`. These are thin orchestrators that chain existing tool functions.
</objective>

<tasks>
Task 1: launch subcommand — preflight + config dump + launch + PID verify + watchdog start
Task 2: closeout subcommand — archive + flush + status update
Task 3: restart subcommand — kill + flush + re-launch
All require --experiment and --confirm for destructive actions.
</tasks>
