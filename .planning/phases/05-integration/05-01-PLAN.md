---
phase: 05-integration
plan: 01
type: execute
wave: 1
depends_on: ["04-01", "04-02"]
files_modified:
  - gigaevo/cli/checkpoint.py
  - tests/cli/test_checkpoint_cmd.py
autonomous: true
requirements:
  - EXT-01
  - EXT-02
---

<objective>
Implement `gigaevo checkpoint --experiment task/name` composite command that runs
status collection, plot generation, and notification dispatch in one shot.

This is the most-used composite command — currently requires 3+ manual steps.
</objective>

<tasks>
Task 1: checkpoint subcommand + tests
  - Collects snapshots via ExperimentMonitor
  - Generates plots via WatchdogPlugin
  - Dispatches StatusUpdate via NotificationDispatcher
  - Supports --no-plots and --no-notify flags
  - Tests mock all external dependencies
</tasks>
