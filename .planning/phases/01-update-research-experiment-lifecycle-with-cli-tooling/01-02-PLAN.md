---
phase: 01-update-research-experiment-lifecycle-with-cli-tooling
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - .claude/skills/experiment-design/SKILL.md
  - .claude/skills/experiment-implement/SKILL.md
  - .claude/skills/experiment-diagnose/SKILL.md
  - .claude/skills/research-scheduler/SKILL.md
  - .claude/skills/auto-optimize-loop/SKILL.md
  - .claude/agents/anomaly-detector.md
  - .claude/agents/project-pm.md
  - .claude/skills/project-pm/SKILL.md
  - .claude/skills/project-pm/board_config.yaml
  - .claude/skills/project-pm/scripts/pm_audit.py
autonomous: true
requirements: [D-01, D-04, D-05, D-06, D-07, D-08]

must_haves:
  truths:
    - "experiment-design SKILL.md has zero PYTHONPATH references"
    - "experiment-implement SKILL.md has zero PYTHONPATH references"
    - "experiment-diagnose SKILL.md has zero PYTHONPATH references (except diagnose.py invocation per D-05)"
    - "research-scheduler SKILL.md has zero resource_manager.py references"
    - "anomaly-detector.md has zero PYTHONPATH references"
    - "project-pm skill directory is deleted entirely"
    - "project-pm agent file is deleted"
    - "No skill references pm_audit.py"
  artifacts:
    - path: ".claude/skills/experiment-design/SKILL.md"
      provides: "Updated skill using gigaevo CLI commands"
      contains: "gigaevo -e"
    - path: ".claude/skills/experiment-implement/SKILL.md"
      provides: "Updated skill using gigaevo CLI commands"
      contains: "gigaevo -e"
    - path: ".claude/skills/experiment-diagnose/SKILL.md"
      provides: "Skill left as-is per D-05 except PYTHONPATH cleanup in non-diagnose.py invocations"
    - path: ".claude/skills/research-scheduler/SKILL.md"
      provides: "Updated skill without resource_manager references"
    - path: ".claude/agents/anomaly-detector.md"
      provides: "Updated agent using gigaevo CLI commands"
      contains: "gigaevo -e"
  key_links:
    - from: ".claude/skills/experiment-design/SKILL.md"
      to: "gigaevo CLI"
      via: "bash code blocks calling gigaevo manifest"
      pattern: "gigaevo.*manifest"
    - from: ".claude/skills/experiment-implement/SKILL.md"
      to: "gigaevo CLI"
      via: "bash code blocks calling gigaevo manifest"
      pattern: "gigaevo.*manifest"
---

<objective>
Update the first batch of experiment lifecycle skills and agents to replace inline Python with `gigaevo` CLI calls. Delete project-pm entirely. Remove resource_manager.py references.

Purpose: Per D-01 and D-07, all skills must use `gigaevo` CLI calls instead of inline Python. Per D-04, project-pm is deleted entirely. Per D-06, resource_manager references are removed. This plan handles experiment-design, experiment-implement, experiment-diagnose, research-scheduler, auto-optimize-loop, anomaly-detector, and the project-pm deletion.

Output: 6 updated skill/agent files + 4 deleted files (project-pm directory + agent).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-update-research-experiment-lifecycle-with-cli-tooling/01-CONTEXT.md
@.planning/phases/01-update-research-experiment-lifecycle-with-cli-tooling/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete project-pm and remove all pm_audit references from skills</name>
  <files>
    .claude/skills/project-pm/SKILL.md,
    .claude/skills/project-pm/board_config.yaml,
    .claude/skills/project-pm/scripts/pm_audit.py,
    .claude/agents/project-pm.md,
    .claude/skills/experiment-design/SKILL.md,
    .claude/skills/experiment-implement/SKILL.md,
    .claude/skills/experiment-launch/SKILL.md,
    .claude/skills/experiment-closeout/SKILL.md
  </files>
  <read_first>
    - .claude/skills/project-pm/SKILL.md (understand what's being deleted)
    - .claude/agents/project-pm.md (understand what's being deleted)
    - .claude/skills/experiment-design/SKILL.md (find pm_audit reference at Step 8a)
    - .claude/skills/experiment-implement/SKILL.md (find pm_audit reference at Step 13a)
    - .claude/skills/experiment-launch/SKILL.md (find pm_audit reference at Step 9a)
    - .claude/skills/experiment-closeout/SKILL.md (find pm_audit reference at Step 9a)
  </read_first>
  <action>
    Per D-04: DELETE the entire project-pm skill and agent.

    1. Delete these files:
       - `.claude/skills/project-pm/SKILL.md`
       - `.claude/skills/project-pm/board_config.yaml`
       - `.claude/skills/project-pm/scripts/pm_audit.py`
       - `.claude/agents/project-pm.md`
    2. Delete the empty directories:
       - `.claude/skills/project-pm/scripts/`
       - `.claude/skills/project-pm/`

    3. Remove pm_audit references from 4 skills (the "Step Xa -- Sync tracking" blocks):

       **experiment-design/SKILL.md** — DELETE the entire "Step 8a -- Sync tracking" section (lines 216-221 that run pm_audit.py --sync). The section header and code block are both removed entirely.

       **experiment-implement/SKILL.md** — DELETE the entire "Step 13a -- Sync tracking" section (lines 324-328 that run pm_audit.py --sync). The section header and code block are both removed entirely.

       **experiment-launch/SKILL.md** — DELETE the entire "Step 9a -- Sync tracking" section (lines 199-203 that run pm_audit.py --sync). The section header and code block are both removed entirely.

       **experiment-closeout/SKILL.md** — DELETE the entire "Step 9a -- Sync tracking and regenerate INDEX.md" section (lines 206-211 that run pm_audit.py --sync). The section header and code block are both removed entirely.

    NOTE: The 4 skills modified here (experiment-design, experiment-implement, experiment-launch, experiment-closeout) are ALSO modified in Task 2 and Plan 03 for inline Python replacement. This task ONLY removes pm_audit sections. The inline Python replacement happens separately.
  </action>
  <verify>
    <automated>cd /mnt/virtual_ai0001071-04017_SR004-nfs1/CFS-SR008/workspace/mathemage/gigaevo-core-internal && test ! -f .claude/skills/project-pm/SKILL.md && test ! -f .claude/agents/project-pm.md && test ! -f .claude/skills/project-pm/scripts/pm_audit.py && ! grep -r "pm_audit" .claude/skills/experiment-design/SKILL.md .claude/skills/experiment-implement/SKILL.md .claude/skills/experiment-launch/SKILL.md .claude/skills/experiment-closeout/SKILL.md 2>/dev/null && echo "PASS: project-pm deleted, pm_audit references removed" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/project-pm/ directory does not exist
    - .claude/agents/project-pm.md does not exist
    - grep -r "pm_audit" across experiment-design, experiment-implement, experiment-launch, experiment-closeout SKILL.md files returns zero matches
    - grep -r "project-pm" across those same files returns zero matches
  </acceptance_criteria>
  <done>project-pm skill and agent completely deleted. All 4 skills that referenced pm_audit.py no longer contain those references.</done>
</task>

<task type="auto">
  <name>Task 2: Update experiment-design, experiment-implement, experiment-diagnose, research-scheduler, auto-optimize-loop, anomaly-detector</name>
  <files>
    .claude/skills/experiment-design/SKILL.md,
    .claude/skills/experiment-implement/SKILL.md,
    .claude/skills/experiment-diagnose/SKILL.md,
    .claude/skills/research-scheduler/SKILL.md,
    .claude/skills/auto-optimize-loop/SKILL.md,
    .claude/agents/anomaly-detector.md
  </files>
  <read_first>
    - .claude/skills/experiment-design/SKILL.md (current full content — find every PYTHONPATH/$GIGAEVO_PYTHON -c block)
    - .claude/skills/experiment-implement/SKILL.md (current full content)
    - .claude/skills/experiment-diagnose/SKILL.md (current full content — per D-05, leave diagnose.py as-is, only fix non-diagnose.py PYTHONPATH lines)
    - .claude/skills/research-scheduler/SKILL.md (current full content — has resource_manager.py reference to remove per D-06)
    - .claude/skills/auto-optimize-loop/SKILL.md (single PYTHONPATH reference in default benchmark command)
    - .claude/agents/anomaly-detector.md (current full content — has PYTHONPATH + load_manifest blocks)
    - gigaevo/cli/manifest_cmd.py (the new CLI subcommands from Plan 01 — to know exact command syntax)
  </read_first>
  <action>
    Replace ALL inline Python snippets that call `tools.experiment.manifest` with equivalent `gigaevo` CLI calls. Per D-01, zero inline Python should remain in skills after this phase.

    **Replacement mapping** (apply to every occurrence in the files listed above):

    | Inline Python pattern | Replacement CLI command |
    |---|---|
    | `$GIGAEVO_PYTHON -c "from tools.experiment.manifest import load_manifest; m = load_manifest('$EXP'); assert m.status == 'X', ..."` | `gigaevo -e "$EXP" manifest gate X` |
    | `$GIGAEVO_PYTHON -c "from tools.experiment.manifest import load_manifest; m = load_manifest('$EXP'); print(f'Status: {m.status}')..."` | `gigaevo -e "$EXP" manifest get status` |
    | `$GIGAEVO_PYTHON -c "from tools.experiment.manifest import set_status; m = set_status('$EXP', 'running')..."` | `gigaevo -e "$EXP" manifest set status running` |
    | `$GIGAEVO_PYTHON -c "from tools.experiment.manifest import update_manifest; def fn(raw): raw['X']['Y'] = Z; update_manifest('$EXP', fn)"` | `gigaevo -e "$EXP" manifest update X.Y Z` |
    | `$GIGAEVO_PYTHON -c "from tools.experiment.manifest import generate_pr_description, load_manifest; ..."` | `gigaevo -e "$EXP" manifest pr-description --push` |
    | `$GIGAEVO_PYTHON -c "from tools.experiment.manifest import load_manifest; m = load_manifest('$EXP'); print(len(m.runs))..."` | `gigaevo -e "$EXP" manifest get runs --format json` (pipe to jq if needed) |
    | `PYTHONPATH=$PROJ $GIGAEVO_PYTHON tools/experiment/generate_launch.py --experiment '$EXP'` | `gigaevo -e "$EXP" generate-launch` (this already exists as a lifecycle command, OR keep the PYTHONPATH invocation if no CLI equivalent exists yet — check first) |
    | `PYTHONPATH=$PROJ $GIGAEVO_PYTHON tools/experiment/preflight_check.py --experiment '$EXP'` | `gigaevo -e "$EXP" preflight` (if exists) OR `gigaevo -e "$EXP" launch --dry-run` (preflight is part of launch) |

    **File-by-file specifics:**

    **experiment-design/SKILL.md:**
    - Step 0: gate check → `gigaevo -e "$EXP" manifest gate new` (but manifest doesn't exist yet for new experiments, so replace with a simple `test ! -f "$PROJ/experiments/$EXP/experiment.yaml"` bash check — this is already mostly what it does)
    - Step 5b: dataset checksums — this is a standalone Python script, NOT a manifest call. Leave as-is (it's computing checksums, not calling manifest).
    - Step 7: status assertion → `gigaevo -e "$EXP" manifest gate preregistered`
    - Step 9: completion check → `gigaevo -e "$EXP" manifest gate preregistered` + additional field checks via `gigaevo -e "$EXP" manifest get stopping_rule`

    **experiment-implement/SKILL.md:**
    - Step 0: gate check → `gigaevo -e "$EXP" manifest gate preregistered`
    - Step 7: resource query — REMOVE the `resource_manager.py` call (per D-06). Replace with a comment: `# Manually pick servers and Redis DBs. See experiments/infrastructure.yaml for available resources.`
    - Step 7: run count query → `gigaevo -e "$EXP" manifest get runs --format json | jq length` (or simpler: just read experiment.yaml directly since this is a design-time step)
    - Step 8: generate_launch.py → keep as `PYTHONPATH=$PROJ $GIGAEVO_PYTHON tools/experiment/generate_launch.py --experiment '$EXP'` if no CLI equivalent exists. Check if `gigaevo -e "$EXP" generate-launch` works.
    - Step 10b: record alignment → `gigaevo -e "$EXP" manifest update treatment_verification.alignment_check_completed true` + `gigaevo -e "$EXP" manifest update treatment_verification.alignment_check_at "$(date -u +%Y-%m-%dT%H:%M:%SZ)"`
    - Step 10 (after treatment): record TV → `gigaevo -e "$EXP" manifest update treatment_verification.completed true` + `gigaevo -e "$EXP" manifest update treatment_verification.completed_at "$(date -u +%Y-%m-%dT%H:%M:%SZ)"`
    - Step 11: smoke test — read run config → `gigaevo -e "$EXP" manifest get runs --format json | jq '.[0]'` to get first run's prefix, db, pipeline, problem. The inline Redis checks after smoke can stay as inline Python (they're Redis queries, not manifest calls).
    - Step 11 end: record smoke → `gigaevo -e "$EXP" manifest update smoke_test.completed true` + `gigaevo -e "$EXP" manifest update smoke_test.completed_at "$(date -u +%Y-%m-%dT%H:%M:%SZ)"` + `gigaevo -e "$EXP" manifest update smoke_test.log_path smoke.log`
    - Step 13: set status → `gigaevo -e "$EXP" manifest set status implemented`
    - Step 15: completion check → `gigaevo -e "$EXP" manifest gate implemented` + field checks via `gigaevo -e "$EXP" manifest get runs --format json`

    **experiment-diagnose/SKILL.md:**
    - Per D-05: Leave diagnose.py invocation (`PYTHONPATH="$PROJ" $GIGAEVO_PYTHON "$PROJ/.claude/skills/experiment-diagnose/scripts/diagnose.py"`) AS-IS. diagnose.py is a standalone diagnostic script, not a manifest wrapper.
    - Step 1: gather run parameters — the `PYTHONPATH=$PROJ $GIGAEVO_PYTHON -c "from tools.experiment.manifest import load_manifest..."` block → `gigaevo -e "EXPERIMENT_NAME" manifest get runs` (tabular output shows label, db, prefix, pipeline, etc.)
    - The `PYTHONPATH` in diagnose.py's actual invocation line in Step 2 stays (it's launching a Python script, not an inline snippet).

    **research-scheduler/SKILL.md:**
    - Step 1: `PYTHONPATH="$PROJ" $GIGAEVO_PYTHON tools/resource_manager.py --check` → DELETE this entire step. Per D-06, resource_manager.py references are removed. Replace with: "Step 1 — Check resources: Manually verify available servers and Redis DBs in `experiments/infrastructure.yaml`. If no free resources, wait for a run to complete."
    - Step 2: inline Python reading IDEAS.yaml — this is NOT a manifest call, it reads IDEAS.yaml directly. Convert to a bash/jq approach or leave as-is. Since IDEAS.yaml parsing is complex YAML, keep as inline Python but remove the PYTHONPATH prefix (it's not needed for yaml parsing with system python). Actually, check if `$GIGAEVO_PYTHON` is used — yes it is. Since this is pure YAML parsing (no tools/ import), replace `PYTHONPATH="$PROJ" $GIGAEVO_PYTHON -c "..."` with just `$GIGAEVO_PYTHON -c "..."` (no PYTHONPATH needed since it only imports yaml and pathlib).
    - Step 4: inline Python updating IDEAS.yaml — same as above, remove PYTHONPATH prefix.

    **auto-optimize-loop/SKILL.md:**
    - Step 0: default benchmark command `PYTHONPATH=. /home/jovyan/envs/evo_fast/bin/python benchmarks/bench_multirun.py --duration 5.0 --sweep` → Replace PYTHONPATH=. with `cd "$PROJ" &&` pattern, or leave as-is if benchmark scripts need PYTHONPATH. This is NOT a manifest/tools call. Replace with: `$GIGAEVO_PYTHON -m tools.benchmark --duration 5.0 --sweep` or keep the existing pattern since it references a different python env (`evo_fast`). The simplest fix: just remove the `PYTHONPATH=.` and rely on the project being installed via `pip install -e .`.

    **anomaly-detector.md:**
    - Step 3: read previous state — inline Python with Redis, no manifest import. Remove PYTHONPATH prefix since it only imports `redis` and `json`.
    - Step 4a: watchdog liveness — `PYTHONPATH="$PROJ" $GIGAEVO_PYTHON -c "...from tools.experiment.manifest import load_manifest..."` → `gigaevo -e "$EXP" manifest get launch.watchdog_pid` to get PID, then bash `kill -0 "$PID" 2>/dev/null` check.
    - Step 4b: completion check — complex inline Python with manifest + Redis. Replace manifest portion with `gigaevo -e "$EXP" manifest get runs --format json` for run specs, then do Redis queries inline. Or keep as-is for the Redis logic part and only replace the `load_manifest` portion.
    - Step 7: watchdog auto-restart — `PYTHONPATH="$PROJ" $GIGAEVO_PYTHON -c "from tools.experiment.manifest import update_manifest..."` → `gigaevo -e "$EXP" manifest update launch.watchdog_pid $NEW_WD_PID`
    - Step 8: write state marker — pure Redis, no manifest. Remove PYTHONPATH prefix.

    **General rules for all files:**
    - Remove ALL `source "$(git rev-parse --show-toplevel)/tools/experiment/skill_env.sh"` lines — these set PYTHONPATH and PROJ which are no longer needed when using CLI
    - Keep `PROJ="$(git rev-parse --show-toplevel)"` where it's used for file paths (e.g., `experiments/$EXP/...`)
    - Remove all `PYTHONPATH="$PROJ"` or `PYTHONPATH=$PROJ` prefixes from command invocations
    - Replace `$GIGAEVO_PYTHON -c "from tools.experiment.manifest import ..."` with `gigaevo -e "$EXP" manifest ...`
    - Keep inline Python that does pure computation (dataset checksums, Redis queries without manifest) but strip the PYTHONPATH prefix if it only imports standard library or pip-installed packages
    - For complex multi-step blocks that mix manifest + Redis + subprocess, split into separate commands: manifest parts → `gigaevo manifest`, Redis parts → can stay as inline Python with just `$GIGAEVO_PYTHON -c "import redis..."` (no PYTHONPATH needed)
  </action>
  <verify>
    <automated>cd /mnt/virtual_ai0001071-04017_SR004-nfs1/CFS-SR008/workspace/mathemage/gigaevo-core-internal && echo "=== PYTHONPATH check ===" && count=$(grep -r "PYTHONPATH" .claude/skills/experiment-design/SKILL.md .claude/skills/experiment-implement/SKILL.md .claude/skills/experiment-diagnose/SKILL.md .claude/skills/research-scheduler/SKILL.md .claude/skills/auto-optimize-loop/SKILL.md .claude/agents/anomaly-detector.md 2>/dev/null | grep -v "diagnose.py" | wc -l) && echo "PYTHONPATH refs (excluding diagnose.py): $count" && echo "=== resource_manager check ===" && rm_count=$(grep -r "resource_manager" .claude/skills/research-scheduler/SKILL.md .claude/skills/experiment-implement/SKILL.md 2>/dev/null | wc -l) && echo "resource_manager refs: $rm_count" && echo "=== pm_audit check ===" && pm_count=$(grep -r "pm_audit" .claude/skills/ .claude/agents/ 2>/dev/null | grep -v "evals/" | wc -l) && echo "pm_audit refs (excluding evals): $pm_count" && if [ "$count" -eq 0 ] && [ "$rm_count" -eq 0 ] && [ "$pm_count" -eq 0 ]; then echo "PASS"; else echo "FAIL"; fi</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "PYTHONPATH" across all 6 files returns 0 matches (excluding diagnose.py script invocation line in experiment-diagnose which stays per D-05)
    - grep -r "resource_manager" across research-scheduler and experiment-implement returns 0 matches
    - grep -r "pm_audit" across all .claude/skills/ and .claude/agents/ (excluding evals/) returns 0 matches
    - Each updated skill contains `gigaevo -e` or `gigaevo manifest` in its bash code blocks
    - experiment-design/SKILL.md contains "gigaevo -e" in gate check steps
    - experiment-implement/SKILL.md contains "gigaevo -e" in gate check and status set steps
    - anomaly-detector.md contains "gigaevo -e" in watchdog liveness and manifest update steps
    - research-scheduler/SKILL.md Step 1 no longer references resource_manager.py
    - experiment-diagnose/SKILL.md Step 1 uses `gigaevo -e` for run parameter gathering
  </acceptance_criteria>
  <done>6 files updated to use gigaevo CLI. Zero PYTHONPATH inline Python in non-diagnose.py contexts. resource_manager.py and pm_audit.py references fully removed from skills.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill markdown -> CLI commands | Skills instruct Claude to run bash commands; replacing Python with CLI maintains same trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | skill file edits | accept | Skills are version-controlled markdown; git diff shows all changes |
| T-01-02-02 | Repudiation | pm_audit deletion | accept | Deletion is per user decision D-04; git history preserves the code |
</threat_model>

<verification>
1. Zero PYTHONPATH references in updated skills (excluding diagnose.py per D-05)
2. Zero resource_manager references in updated skills
3. Zero pm_audit references in any skill or agent (excluding evals/)
4. project-pm directory and agent fully deleted
5. Every updated skill still has the same step structure (same step numbers, same gates, same flow)
</verification>

<success_criteria>
- All 6 skill/agent files updated with gigaevo CLI calls replacing inline Python
- project-pm skill directory + agent deleted (4 files removed)
- pm_audit references removed from 4 skills
- resource_manager references removed from 2 skills
- Skill step structure preserved (no steps lost, no gates weakened)
</success_criteria>

<output>
After completion, create `.planning/phases/01-update-research-experiment-lifecycle-with-cli-tooling/01-02-SUMMARY.md`
</output>
