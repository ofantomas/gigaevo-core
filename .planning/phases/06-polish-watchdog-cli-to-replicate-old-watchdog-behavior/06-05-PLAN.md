---
phase: 06-polish-watchdog-cli-to-replicate-old-watchdog-behavior
plan: 05
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - .claude/skills/experiment-design/SKILL.md
  - .claude/skills/experiment-implement/SKILL.md
autonomous: false
requirements: []

must_haves:
  truths:
    - "experiment-design skill proposes a monitoring section in the design doc with suggested plots, metrics, and alert thresholds"
    - "experiment-implement skill confirms or adjusts monitoring config when writing experiment.yaml"
    - "Both skills reference the WatchdogSection schema fields from manifest_schema.py"
  artifacts:
    - path: ".claude/skills/experiment-design/SKILL.md"
      provides: "Monitoring config proposal in design docs"
      contains: "watchdog"
    - path: ".claude/skills/experiment-implement/SKILL.md"
      provides: "Watchdog config step when writing experiment.yaml"
      contains: "watchdog"
  key_links:
    - from: ".claude/skills/experiment-design/SKILL.md"
      to: "gigaevo/monitoring/manifest_schema.py"
      via: "references WatchdogSection schema"
      pattern: "WatchdogSection"
    - from: ".claude/skills/experiment-implement/SKILL.md"
      to: "gigaevo/monitoring/manifest_schema.py"
      via: "writes watchdog section in experiment.yaml"
      pattern: "watchdog"
---

<objective>
Update experiment-design and experiment-implement skills to integrate watchdog monitoring configuration into the experiment lifecycle. Design proposes monitoring config; implement confirms and writes it to experiment.yaml. Visual verification confirms plot quality matches old watchdog.

Purpose: Per D-21 and D-22, the experiment lifecycle agents should automatically propose and wire monitoring configuration so researchers get proper watchdog behavior without manual YAML editing. D-19 and D-20 require visual verification of plot quality.

Output: Updated SKILL.md files for experiment-design and experiment-implement
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-polish-watchdog-cli-to-replicate-old-watchdog-behavior/06-CONTEXT.md
@.planning/phases/06-polish-watchdog-cli-to-replicate-old-watchdog-behavior/06-02-SUMMARY.md

@.claude/skills/experiment-design/SKILL.md
@.claude/skills/experiment-implement/SKILL.md
@gigaevo/monitoring/manifest_schema.py

<interfaces>
From gigaevo/monitoring/manifest_schema.py (created in Plan 01):
```python
class PlotCommand(BaseModel):
    command: str  # "arms-race", "comparison", "trajectory"
    args: dict[str, Any] = {}
    output_name: str = ""
    caption: str = ""

class AlertThresholds(BaseModel):
    invalidity_rate: float = 0.75
    stagnation_window: int = 10
    generation_gap_threshold: int = 5

class WatchdogSection(BaseModel):
    plugin: str | None = None
    plot_commands: list[PlotCommand] = []
    plot_metrics: list[str] = []
    alert_thresholds: AlertThresholds = AlertThresholds()
    poll_interval_s: int = 3600
    plot_retries: int = 3
    plot_retry_delay_s: int = 30
    rolling_comment_threshold_hours: int = 24
    checkpoint_milestones: list[float] = [0.1, 0.2, 0.5, 1.0]
    no_proxy_hosts: list[str] = []
```

Plugin auto-detection rules:
- Runs with "pop_a"/"pop_b" prefixes -> adversarial plugin
- Runs with "prompt_evolution" in prefix -> prompt_coevo plugin
- Everything else -> solo plugin
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add monitoring config proposal to experiment-design skill</name>
  <files>.claude/skills/experiment-design/SKILL.md</files>
  <read_first>
    - .claude/skills/experiment-design/SKILL.md
    - gigaevo/monitoring/manifest_schema.py (WatchdogSection, PlotCommand, AlertThresholds)
  </read_first>
  <action>
Add a new step/section to the experiment-design SKILL.md (per D-21) that instructs the agent to propose a monitoring configuration in the design document. Insert this after the existing "Runs" or "Configuration" section in the design template.

Add the following content to the appropriate step in the skill workflow:

**Section: "Step N: Monitoring Configuration"** (or append to the existing experiment config step):

```markdown
### Monitoring Configuration

Based on the experiment type, propose a `watchdog:` section for experiment.yaml:

**For solo MAP-Elites experiments:**
watchdog:
  plugin: solo
  plot_metrics: [primary_metric_from_metrics.yaml]
  plot_commands:
    - command: comparison
      args: {metric: primary_metric, smoothing: ema, window: 5}
      caption: "Fitness comparison"
  alert_thresholds:
    invalidity_rate: 0.75
    stagnation_window: 10

**For adversarial pair experiments:**
watchdog:
  plugin: adversarial
  plot_metrics: [primary_metric]
  plot_commands:
    - command: arms-race
      args: {metric: primary_metric, paired: "G_label:D_label,..."}
      caption: "Arms-race dynamics"
    - command: comparison
      args: {metric: primary_metric, smoothing: ema, window: 10, annotate-frontier: true, no-frontier-for: "D_labels"}
      caption: "All runs vs SOTA"
  alert_thresholds:
    invalidity_rate: 0.75
    stagnation_window: 10

**For prompt co-evolution experiments:**
watchdog:
  plugin: prompt_coevo
  plot_metrics: [primary_metric]
  plot_commands:
    - command: comparison
      args: {metric: primary_metric, smoothing: ema, window: 5}
      caption: "Code population comparison"
  alert_thresholds:
    invalidity_rate: 0.75
    stagnation_window: 10

Include this proposed config in the Monitoring section of the design document (01_design.md). Ask the researcher:
1. Which metrics should be plotted? (default: primary metric from metrics.yaml)
2. What alert thresholds? (defaults shown above are usually fine)
3. Any custom plot commands beyond the defaults?

Reference: gigaevo/monitoring/manifest_schema.py (WatchdogSection, PlotCommand, AlertThresholds)
```

Also add "Monitoring configuration proposed in design doc" to the skill's output/deliverables list.
  </action>
  <verify>
    <automated>cd /mnt/virtual_ai0001071-04017_SR004-nfs1/CFS-SR008/workspace/mathemage/gigaevo-core-internal && grep -c "watchdog" .claude/skills/experiment-design/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md contains "Monitoring Configuration" or "watchdog" section
    - SKILL.md contains YAML example for solo, adversarial, and prompt_coevo watchdog config
    - SKILL.md references WatchdogSection or manifest_schema.py
    - SKILL.md includes researcher questions about metrics and thresholds
  </acceptance_criteria>
  <done>experiment-design skill proposes monitoring config in design docs, with templates for all 3 experiment types and researcher questions for customization.</done>
</task>

<task type="auto">
  <name>Task 2: Add watchdog config step to experiment-implement skill</name>
  <files>.claude/skills/experiment-implement/SKILL.md</files>
  <read_first>
    - .claude/skills/experiment-implement/SKILL.md
    - .claude/skills/experiment-design/SKILL.md (after Task 1, to see what design proposes)
    - gigaevo/monitoring/manifest_schema.py
  </read_first>
  <action>
Add a new step to the experiment-implement SKILL.md (per D-22) that instructs the agent to confirm/adjust the monitoring config when writing experiment.yaml.

Insert this step in the "Write experiment.yaml" section or after the runs are configured:

```markdown
### Step N: Configure Watchdog Monitoring

When writing experiment.yaml, include the watchdog section:

1. Read the design doc (01_design.md) for the proposed monitoring config
2. Auto-detect plugin from run prefixes:
   - Runs with pop_a/pop_b prefixes -> plugin: adversarial
   - Runs with prompt_evolution in prefix -> plugin: prompt_coevo
   - Otherwise -> plugin: solo
3. Confirm with researcher: "The design proposes these plot metrics and alert thresholds. Should I adjust?"
   - Which metrics to plot (from metrics.yaml)
   - Alert thresholds (invalidity_rate, stagnation_window)
   - Plot commands (arms-race/comparison/trajectory with specific args)
4. Write the watchdog section in experiment.yaml with confirmed values
5. Validate: gigaevo manifest gate experiment_name

Reference: gigaevo/monitoring/manifest_schema.py (WatchdogSection schema)
```

Also add "watchdog section configured in experiment.yaml" to the skill's output/deliverables list.
  </action>
  <verify>
    <automated>cd /mnt/virtual_ai0001071-04017_SR004-nfs1/CFS-SR008/workspace/mathemage/gigaevo-core-internal && grep -c "watchdog" .claude/skills/experiment-implement/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md contains "Watchdog" or "watchdog" configuration step
    - SKILL.md contains plugin auto-detection rules (pop_a/pop_b to adversarial)
    - SKILL.md contains "Confirm with researcher" or similar interaction prompt
    - SKILL.md references gigaevo manifest gate for validation
    - SKILL.md references manifest_schema.py or WatchdogSection
  </acceptance_criteria>
  <done>experiment-implement skill includes watchdog config step: auto-detect plugin, confirm with researcher, write watchdog section, validate with manifest gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of watchdog plot quality against reference plots</name>
  <files>experiments/heilbron/asymmetric-iterations/plots/</files>
  <action>
Per D-19 and D-20, visually inspect the produced plots from the new watchdog against reference plots from previous adversarial experiments. The watchdog plugins now delegate to gigaevo plot arms-race and gigaevo plot comparison CLI commands. These commands produce publication-quality time-series plots with EMA smoothing, confidence bands, frontier dashed lines, SOTA baselines, proper legends, and multi-panel arms-race layout. The human verifier confirms these features match the reference plots and flags any visual regressions.
  </action>
  <verify>
    <automated>cd /mnt/virtual_ai0001071-04017_SR004-nfs1/CFS-SR008/workspace/mathemage/gigaevo-core-internal && ls experiments/heilbron/asymmetric-iterations/plots/*.png 2>/dev/null | head -5</automated>
  </verify>
  <done>Human confirms produced plots match reference quality: time-series curves (not bars), EMA smoothing, confidence bands, frontier dashes, SOTA baselines, proper legends, dual-panel arms-race.</done>
  <what-built>
Watchdog plugins delegate to gigaevo plot CLI commands. The produced plots should match the quality of the reference plots from previous experiments:
- experiments/heilbron/asymmetric-iterations/plots/arms_race_hour_013.png (8-run arms-race)
- experiments/heilbron/asymmetric-iterations/plots/g_fitness_hour_013.png (8-run comparison)
  </what-built>
  <how-to-verify>
1. If the experiment is still running, generate a live plot:
   gigaevo -e heilbron/asymmetric-iterations plot arms-race --metric actual_fitness --paired "A1_G:A1_D,A2_G:A2_D,C1_G:C1_D,C2_G:C2_D" -o /tmp/watchdog_plot_test/

2. Compare against reference plots at experiments/heilbron/asymmetric-iterations/plots/

3. Verify quality features (per D-20):
   - Time-series curves (NOT bar charts)
   - EMA smoothing with confidence bands
   - Frontier (best) dashed lines alongside mean
   - SOTA baseline reference lines (if configured)
   - Proper legends with condition labels
   - Multi-panel layout for arms-race (G panel + D panel)

4. Any visual regression from reference plots is blocking.
  </how-to-verify>
  <resume-signal>Type "approved" if plots match reference quality, or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill files -> Claude agent | Skills are prompts that guide agent behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | Tampering | SKILL.md content | accept | Skills are committed to repo and reviewed in PRs. No production code execution. |
</threat_model>

<verification>
1. experiment-design skill includes monitoring config proposal
2. experiment-implement skill includes watchdog config step
3. Both skills reference WatchdogSection schema
4. Visual plot quality matches reference plots (human verified)
</verification>

<success_criteria>
- experiment-design skill proposes monitoring config with type-specific templates
- experiment-implement skill writes watchdog section to experiment.yaml with researcher confirmation
- Visual verification confirms plot quality matches old watchdog output
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-watchdog-cli-to-replicate-old-watchdog-behavior/06-05-SUMMARY.md`
</output>
