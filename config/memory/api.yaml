# @package _global_
# Memory selection via API backend (config/memory_backend.yaml controls api.use_api etc).
memory_provider:
  _target_: gigaevo.memory.provider.SelectorMemoryProvider
  max_cards: 3
  checkpoint_dir: ${checkpoint_dir}
  namespace: ${namespace}
