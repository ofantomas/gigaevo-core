# @package _global_
# Memory selection via local backend (config/memory_backend.yaml controls backend details).
memory_provider:
  _target_: gigaevo.memory.provider.SelectorMemoryProvider
  max_cards: 3
  checkpoint_dir: ${checkpoint_dir}
  namespace: ${namespace}
