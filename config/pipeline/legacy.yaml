# @package _global_
#
# Legacy v1 one-shot pipeline using DefaultPipelineBuilder. Kept for
# back-compat with v1.x users; `pipeline=standard` now points at
# IntraMemoryPipelineBuilder (per-parent lineage card + intra-memory).
# Opt in explicitly via `pipeline=legacy` to restore the old behavior.

evolution_context:
  _target_: gigaevo.entrypoint.evolution_context.EvolutionContext
  problem_ctx: ${problem_context}
  llm_wrapper: ${ref:llm}
  storage: ${redis_storage}
  prompts_dir: ${prompts.dir}
  prompt_fetcher: ${prompt_fetcher}
  memory_provider: ${memory_provider}

pipeline_builder:
  _target_: gigaevo.entrypoint.default_pipelines.DefaultPipelineBuilder
  ctx: ${evolution_context}
  stage_timeout: ${stage_timeout}
  dag_timeout: ${dag_timeout}
  max_parallel: ${dag_concurrency}
  max_insights: ${max_insights}
  max_code_length: ${max_code_length}

dag_blueprint:
  _target_: gigaevo.config.helpers.build_dag_from_builder
  builder: ${pipeline_builder}
