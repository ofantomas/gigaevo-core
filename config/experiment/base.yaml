# @package _global_

# Base Experiment - Simple Single Island Setup
# Perfect for getting started and local development

defaults:
  - /constants: base
  - /redis: default
  - /llm: single
  - /prompts: default
  - /prompt_fetcher: fixed
  - /evolution: default
  - /algorithm: single_island
  - /pipeline: auto
  - /runner: default
  - /scheduling: fifo
  - /loader: directory
  - /logging: tensorboard
  - /metrics: default
