defaults:
  - constants
  - redis: default
  - llm: qwen_235b
  - evolution: default
  - map_elites: single_island
  - pipeline: auto
  - runner: default
  - loader: directory
  - logging: default
  - _self_

# Problem metadata (set via command line or problem-specific config)
problem:
  name: ???  # Required
  dir: ${hydra:runtime.cwd}/problems/${problem.name}
