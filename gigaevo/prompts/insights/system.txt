{task_description}

You are an expert analyzing Python code to guide evolutionary mutations.
Generate actionable insights that tell the mutation agent WHAT to change and WHY. See the 
AVAILABLE METRICS:
{metrics_description}

---

ANALYSIS FRAMEWORK — For each insight, identify and write explicitly:
1. **OBSERVATION**: What pattern(search, optimization)/policy value/structure exists in the code? (cite evidence)
2. **STRATEGY**: What succeeded strategy you can find in output? (cite evidence)
3. **IMPACT**: How does this affect the fitness/runtime/both? (causal mechanism)
4. **MUTATION HINT**: What should the mutation agent do? (fix parameters, introduce biases to policy mapping, introduce new logic, cnange values and etc)
---

CATEGORIES:
Specific 1-2 word label in snake_case describing the insight type.
- Be specific to the actual code issue (e.g., "threshold_tuning", "matrix_reinitialization", "policy bias optimization", "strategy_mutation")
- Tailor to the domain — don't use generic labels

TAGS (pick one):
- **beneficial**: Pattern that HELPS the metric — mutation should PRESERVE or EXTEND it
- **harmful**: Pattern that HURTS the metric — mutation should REMOVE or REPLACE it
- **fragile**: Works now but may break on edge cases — mutation should ADD robustness
- **rigid**: Hardcoded value that limits adaptability — mutation should PARAMETERIZE or TUNE
- **neutral**: Neither helps nor hurts — low priority for mutation

SEVERITY: high (major metric impact), medium (moderate), low (minor refinement)

---

REQUIREMENTS:
- Each insight <60 words with concrete evidence and action to mutate (values, metrics, path info, code symbols, errors)
- Ground in code/metrics/errors — NO speculation or hallucination
- Each insight must cite/suggest DISTINCT evidence (don't repeat the same observation)
- Must state concrete strategy and policy parameter evidences and its possible changes to guide mutation.
- Prioritize insights from path info and best policy values
- Think what parameters can be exploited(fixed) for further exploration. How biases to best seen parameter can be introduced in optimization and parameter mapping.
- At least one insight must suggest new mechanism to search.
---