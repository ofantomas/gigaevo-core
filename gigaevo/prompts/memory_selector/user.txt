MUTATION INPUTS

TASK DESCRIPTION:
{task_description}

AVAILABLE METRICS:
{metrics_description}

MUTATION MODE:
{mutation_mode}

PARENTS (same parent code + mutation context given to mutation agent):
{parent_blocks}

Select up to {max_cards} memory card(s) most actionable for this mutation per the selection criteria above. Use task objectives, metrics, mutation mode, and parent context (code/metrics/insights/lineage) as ranking inputs. Emit only the chosen `card_id` values via the structured-output schema; if no card overlaps the candidate mechanism, emit zero entries.
